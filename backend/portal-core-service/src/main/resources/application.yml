server:
  port: 8134

portal-core:
  test-controls:
    enabled: false
  http-smoke:
    gateway-base-url: http://127.0.0.1:8125
    timeout-ms: 1500

guide:
  test-controls:
    enabled: ${portal-core.test-controls.enabled:false}

material:
  test-controls:
    enabled: ${portal-core.test-controls.enabled:false}
