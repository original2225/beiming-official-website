server:
  port: 8133

ops-core:
  test-controls:
    enabled: false

ops-control:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

cloudreve-sync:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

backup-recovery:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

alerting:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

plugin-integration:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

ops-image-market:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}
