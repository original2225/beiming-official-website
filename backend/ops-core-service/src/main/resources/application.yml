server:
  port: 8133

ops-core:
  trusted-gateway:
    internal-signing-secret: local-test-gateway-signing-secret
  http-smoke:
    gateway-base-url: http://127.0.0.1:8125
    self-base-url: http://127.0.0.1:8133
    timeout-ms: 1500
  test-controls:
    enabled: false

ops-control:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

cloudreve-sync:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

backup-recovery:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

alerting:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

plugin-integration:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

cross-platform-notification:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}

ops-image-market:
  test-controls:
    enabled: ${ops-core.test-controls.enabled:false}
