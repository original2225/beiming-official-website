# AGENTS.md

本项目中文名是北冥官网，英文正式名是 Beiming Official Website，仓库名使用 `beiming-official-website`。

开发前必须先读取本文件、`docs/api-reference.md` 和 `docs/system-design.md`。

每个模块开发前，必须先读取所有与该模块相关的正式文档、接口契约、现有代码、配置、路由、数据结构和测试，再开始设计或实现。没有读完必要参考文档时，不得直接写代码。

每个模块正式开发必须遵守固定闭环：先根据 `docs/api-reference.md`、`docs/system-design.md` 和该模块相关正式资料补齐总 API 文档中的模块章节，再根据 API 文档补齐自动化测试用例，然后才允许编码。开发计划只用于确定先后顺序和维护节奏，不能替代 API 文档、测试用例或验收标准。

之后每个模块都必须按同一顺序开发，不得跳步：先补齐总 API 文档中的模块接口，再补齐该模块全部 API 的自动化测试用例，目标是让所有测试用例全部通过；再根据 API 文档在兼容已有模块的前提下开发，开发过程不得修改已有稳定模块结构，不得破坏已有接口、配置、路由、数据结构、响应格式、认证方式、端口、环境变量和测试；开发完成后必须执行全部相关自动化测试；测试不通过时必须根据失败原因修复 bug、实现、API 文档或测试用例，并持续复测，直到所有 bug 都修完、所有测试用例全部通过。只有完成 API 文档、自动化测试红灯验证、兼容开发、开发后全量测试、失败修复和最终全绿后，才算该模块开发完成。

API 文档只保留 `docs/api-reference.md`。每个模块必须在总 API 文档中维护自己的清晰章节，共享规则也写入总 API 文档，不再维护历史微服务独立契约文件。API 文档必须明确接口路径、方法、认证要求、权限点、请求字段、响应字段、错误码、分页规则、幂等规则、状态流转、失败降级、审计要求和验收口径。没有正式 API 文档时，不得编写测试用例或生产代码。

测试用例必须根据 API 文档生成，并覆盖该模块的全部 API。测试用例必须完整覆盖每个接口的成功路径、字段校验、认证失败、权限不足、资源不存在、状态冲突、幂等或并发边界、状态流转、失败降级、审计要求和模块验收口径。测试目标是让所有测试用例最终全部通过，不是抽样验证，也不是只覆盖主要接口。

测试用例完成后，必须先确认自动化测试会因为接口未实现或行为未满足而失败，再开始根据 API 文档编码。编码时必须以 API 文档和测试用例作为完成判定。编码完成后必须运行该模块的全部相关测试。

每次测试完成后，无论测试失败还是成功，都必须保留可追溯记录。记录内容至少包括测试时间、测试范围、执行命令、运行环境、成功或失败结果、失败原因或成功覆盖点、修复动作、复测结果和剩余风险。可复用的验收规则再提炼进正式 API 文档，不得把本地测试文档提交到仓库。

之后所有数据库开发、数据库结构调整、持久化适配和相关测试，都必须在 Docker Desktop WSL 或服务器同等真实容器环境中完成。不得只依赖本机进程、H2、MockMvc 或未容器化数据库作为完成依据。容器环境至少包含 `beiming-backend` 和 `beiming-postgres`，并且两个容器必须处于 `healthy` 状态；涉及数据库写入、迁移、审计、幂等、请求日志、主读或状态流转的验收，必须通过容器内统一后端访问容器内 PostgreSQL 后再验证。

测试不通过时，必须根据失败原因修复实现、API 文档或测试用例，直到所有 bug 都修完，所有相关测试用例全部通过。不能跳过失败测试，不能只手工验证，不能为了通过测试降低正式需求、系统设计或 API 契约。

只有当该模块的全部 API 都已按 API 文档实现，并且全部测试用例都有对应自动化验证，所有相关测试全部通过后，才算该模块开发完成。当前模块没有完成这个闭环前，不得开始下一个模块。

整个项目必须按依赖顺序开发。推荐顺序固定为：基础契约和工程骨架、`auth`、`profile`、`notification`、`content`、`server-status`、`resource`、`admin`、`onboarding`、`exam`、`whitelist`、`attendance`、`community`、`activity`、`calendar`、`changelog`、前端适配、`ops-control`、`node-daemon`、P3 扩展。没有用户明确确认时，不得跳过前置模块先开发后置模块。

前序模块完成后，它的 API 文档章节、自动化测试、响应格式、错误码、认证方式、数据归属和已通过实现默认进入稳定状态。后序模块必须通过前序模块的正式 API 或后端入口提供的认证上下文进行适配，不能直接读取前序模块数据库，不能要求前序模块为了后序模块回头改已稳定接口，不能把后序模块逻辑塞进前序模块。

后序模块发现前序模块确实缺少必要能力时，必须把它当作前序模块的新变更处理：先说明兼容变更原因，再更新前序模块 API 文档章节，补充或修改自动化测试，确认测试失败，再修改前序模块实现并跑通前序模块全部相关测试。前序模块重新闭环后，后序模块才能继续适配。

模块适配方向必须固定为后序适配前序。例如 `profile` 适配 `auth`，只能读取 `auth` 的当前用户、角色、能力点、Minecraft 绑定状态和用户快照，不能让 `auth` 直接写成员档案。`whitelist` 适配 `profile`、`exam`、`attendance` 和 `notification`，审核通过后只能调用它们的正式接口，不能直接改它们的数据。`ops-control` 适配 `auth` 和 `admin`，真实服务器操作必须交给 `node-daemon`，不能混入玩家资源服务或服务器状态展示服务。

前端不得先行吞业务。前端只能在对应后端模块 API 和自动化测试闭环后进行适配，不能写死首页内容、服务器状态、资源数据、用户权限、审核状态或后台操作结果。接口失败时按对应 API 契约做局部降级。

一次只能开发一个模块。没有用户明确确认时，不得修改其他模块，不得顺手重构无关代码。

开发下一个模块前，必须先检索并理解现有模块的接口、配置、数据结构、路由和测试，确保新模块兼容现有边界。新模块不能破坏已有接口、端口、环境变量、响应格式、认证方式和构建脚本。

前端上传或确认后的文件不能乱动。没有用户明确要求时，不得修改前端页面、样式、资源文件或构建配置。

贯穿整个项目开发的正式规范、接口契约、模块边界、验收口径和对外说明，必须放在仓库内的 `docs/`，并随仓库上传。

指导文档、开发手册、阶段计划、本地测试记录、AI 对话交接文档、临时分析文档和本地模块开发指导文档，默认不放入仓库正式文档。确需本地暂存时可以放在 `.local-docs/`，这个目录必须被 `.gitignore` 忽略，不得上传到 GitHub。多人协作时，每个人都可以在自己本地保留 `.local-docs/`，但不能提交它。

`.local-docs/` 只允许保存仍有当前参考价值的本地资料。阶段资料完成使用后，应把可复用经验写入 `docs/` 中的正式文档，再清理对应本地文件。清理时只能逐个删除明确路径的文件，不能删除整个目录，不能使用递归删除命令。

仓库内的 `docs/` 只允许保留 `docs/api-reference.md` 和 `docs/system-design.md`。新增或恢复其他 `docs/` 文件前必须先获得用户明确确认。

发现本地开发文档误放进可提交路径时，应移动到 `.local-docs/`，并确认 `git status` 没有把它列为待提交文件。

每次进行开发、修改、重构、删除、迁移、依赖调整、配置调整或文档调整，都必须留下可追溯记录。动手前先记录当前分支和 `git status --short --branch`。完成一个明确小步后及时提交 Git commit，提交信息要说明做了什么。不能把多个无关修改塞进同一个提交。

高风险操作前必须先建立可回退点。包括删除文件、迁移目录、重命名模块、重置架构、大规模格式化、升级依赖、改构建脚本和改数据库结构。可回退点可以是新分支、临时 tag 或明确的备份提交。操作后要确认 `git status` 和关键文件清单，确保之后能回到操作前的整个项目状态。

禁止批量删除文件或目录。不要使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。需要删除文件时，只能一次删除一个明确路径的文件。需要批量删除时，必须停止并询问用户。

不要提交 `.idea/`、`node_modules/`、`dist/`、`target/`、日志、本地运行数据、真实密码、真实 token 或本地环境文件。

每次动代码前先检查 `git status --short --branch`。如果发现与当前任务无关的改动，不要回滚，不要覆盖，直接避开。
