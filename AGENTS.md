# AGENTS.md

本项目中文名是北冥官网，英文正式名是 Beiming Official Website，仓库名使用 `beiming-official-website`。

开发前必须先读取本文件、`docs/requirements.md`、`docs/system-design.md` 和 `docs/development-governance.md`。

每个微服务或模块开发前，必须先读取所有与该微服务或模块相关的正式文档、接口契约、现有代码、配置、路由、数据结构和测试，再开始设计或实现。没有读完必要参考文档时，不得直接写代码。

每个微服务或模块正式开发前，必须先根据 `docs/requirements.md`、`docs/system-design.md`、`docs/development-governance.md` 和该模块相关正式资料编写或补齐正式 API 文档。API 文档必须放在 `docs/`，并明确接口路径、方法、认证要求、权限点、请求字段、响应字段、错误码、分页规则、幂等规则、状态流转、失败降级、审计要求和验收口径。没有正式 API 文档时，不得进入测试用例编写或编码。

API 文档完成后，必须先基于 API 文档生成覆盖完整契约的自动化测试用例。测试用例至少覆盖成功路径、字段校验、认证失败、权限不足、资源不存在、状态冲突、幂等或并发边界、审计要求和模块验收口径。没有完成测试用例时，不得编写生产代码。

编码必须以 API 文档和测试用例为准。最终目标是完全满足正式需求、系统设计和 API 文档，同时让所有自动化测试用例通过。发现实现、测试和 API 文档不一致时，必须先判断哪一方违背正式需求和系统设计，再修正文档或测试，不能为了通过测试随意降低契约。

开发计划只用于确定模块先后顺序和维护节奏，不能替代 API 文档、测试用例或验收标准。进入某个微服务或模块开发时，第一个正式产出必须是该模块 API 文档，不能只写笼统开发计划后直接编码。

API 文档完成后，必须根据 API 文档生成覆盖全部接口的契约测试和自动化测试用例。测试文档或测试代码必须覆盖该模块全部 API 的成功路径、失败路径、权限路径、状态流转、审计要求和边界条件。测试用例的目标不是抽样验证，而是作为接口是否完成的基准。

测试用例生成后，必须先确认测试会因为接口未实现或行为未满足而失败，再开始编码。编码完成后必须运行全部相关测试。测试不通过时，必须根据失败原因修复实现、API 文档或测试用例，直到所有相关测试通过。不能跳过失败测试，不能只手工验证，不能为了通过测试降低正式需求、系统设计或 API 契约。

每完成一个微服务或模块后，必须先确认该模块所有 API 均已按文档实现，所有契约测试和自动化测试均已通过，再进入下一个微服务或模块。第一个微服务没有按文档和测试闭环完成时，不得开始下一个微服务。

一次只能开发一个模块。没有用户明确确认时，不得修改其他模块，不得顺手重构无关代码。

开发下一个模块前，必须先检索并理解现有模块的接口、配置、数据结构、路由和测试，确保新模块兼容现有边界。新模块不能破坏已有接口、端口、环境变量、响应格式、认证方式和构建脚本。

前端上传或确认后的文件不能乱动。没有用户明确要求时，不得修改前端页面、样式、资源文件或构建配置。

贯穿整个项目开发的正式规范、接口契约、模块边界、验收口径和对外说明，必须放在仓库内的 `docs/`，并随仓库上传。

指导文档、开发手册、阶段计划、本地测试记录、AI 对话交接文档、临时分析文档和微服务开发指导文档，默认放在仓库内的 `.local-docs/`。这个目录必须被 `.gitignore` 忽略，不得上传到 GitHub。多人协作时，每个人都可以在自己本地保留 `.local-docs/`，但不能提交它。

`.local-docs/` 只允许保存仍有当前参考价值的本地资料。阶段资料完成使用后，应把可复用经验写入 `docs/` 中的正式文档，再清理对应本地文件。清理时只能逐个删除明确路径的文件，不能删除整个目录，不能使用递归删除命令。

仓库内的 `docs/` 只允许保留正式项目文档，例如需求文档、系统设计文档、接口契约和对外说明。新增 `docs/` 文件前必须先确认它是正式要上传的文档，不是本地开发指导。

发现本地开发文档误放进可提交路径时，应移动到 `.local-docs/`，并确认 `git status` 没有把它列为待提交文件。

每次进行开发、修改、重构、删除、迁移、依赖调整、配置调整或文档调整，都必须留下可追溯记录。动手前先记录当前分支和 `git status --short --branch`。完成一个明确小步后及时提交 Git commit，提交信息要说明做了什么。不能把多个无关修改塞进同一个提交。

高风险操作前必须先建立可回退点。包括删除文件、迁移目录、重命名模块、重置架构、大规模格式化、升级依赖、改构建脚本和改数据库结构。可回退点可以是新分支、临时 tag 或明确的备份提交。操作后要确认 `git status` 和关键文件清单，确保之后能回到操作前的整个项目状态。

禁止批量删除文件或目录。不要使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。需要删除文件时，只能一次删除一个明确路径的文件。需要批量删除时，必须停止并询问用户。

不要提交 `.idea/`、`node_modules/`、`dist/`、`target/`、日志、本地运行数据、真实密码、真实 token 或本地环境文件。

每次动代码前先检查 `git status --short --branch`。如果发现与当前任务无关的改动，不要回滚，不要覆盖，直接避开。
