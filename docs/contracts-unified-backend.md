# 北冥官网 unified-backend API 契约

版本：1.31

## 文档定位

本文档是 `unified-backend-service` 统一后端候选入口的正式 API 契约。它只描述统一后端候选入口自有 API、in-process 挂载规则、readiness、HTTP smoke、边界和验收口径，不复制 `auth`、`profile`、`notification`、`content`、`server-status`、`resource`、`admin`、`onboarding`、`exam`、`whitelist`、`attendance`、`community`、`activity`、`calendar`、`changelog`、`ops-control`、`cloudreve-sync`、`backup-recovery`、`alerting`、`plugin-integration`、`cross-platform-notification`、`ops-image-market`、`guide`、`material`、`online-map`、`business-core`、`admission-core`、`engagement-core`、`ops-core`、`portal-core` 或 `api-gateway` 的业务契约。

本文档继承 `docs/contracts-common.md`。统一响应格式、认证头、请求编号、错误结构、分页、角色、能力点、审计字段、风险等级和通用错误码均以公共契约为准。被挂载模块的原路径、响应格式、错误码、认证方式和测试口径仍以各自正式契约为准。

## 职责边界

`unified-backend-service` 是统一后端并行候选入口，端口固定为 `8135`，部署模式为 `CANDIDATE_PARALLEL_ENTRYPOINT`。它不替代当前生产入口，当前用于本地验证 `api-gateway`、`business-core`、`admission-core`、`engagement-core`、`ops-core` 与 `portal-core` 能否在同一 Spring Boot 进程内装配。

候选入口必须在同一进程内挂载 `api-gateway` 自有 API、`business-core` 自有 API、`admission-core` 自有 API、`engagement-core` 自有 API、`ops-core` 自有 API、`portal-core` 自有 API、第一批业务路由 `auth`、`profile`、`notification`、`content`、`server-status`、`resource`、`admin`，第二批入服准入路由 `onboarding`、`exam`、`whitelist`、`attendance`，第三批社区运营路由 `community`、`activity`、`calendar`、`changelog`，第四批和第六期运维通知路由 `ops-control`、`cloudreve-sync`、`backup-recovery`、`alerting`、`plugin-integration`、`cross-platform-notification`、`ops-image-market`，以及第五批门户体验路由 `guide`、`material` 和 `online-map`。通过候选入口访问 `/api/v1/gateway/**`、`/api/v1/business-core/**`、`/api/v1/admission-core/**`、`/api/v1/engagement-core/**`、`/api/v1/ops-core/**`、`/api/v1/portal-core/**`、`/api/v1/auth/**`、`/api/v1/profile/**`、`/api/v1/notifications/**`、`/api/v1/content/**`、`/api/v1/server-status/**`、`/api/v1/resources/**`、`/api/v1/admin/**`、`/api/v1/onboarding/**`、`/api/v1/exams/**`、`/api/v1/whitelist/**`、`/api/v1/attendance/**`、`/api/v1/community/**`、`/api/v1/activity/**`、`/api/v1/calendar/**`、`/api/v1/changelog/**`、`/api/v1/ops-control/**`、`/api/v1/cloudreve-sync/**`、`/api/v1/backup-recovery/**`、`/api/v1/alerting/**`、`/api/v1/plugin-integration/**`、`/api/v1/cross-platform-notification/**`、`/api/v1/ops-image-market/**`、`/api/v1/guides/**`、`/api/v1/materials/**` 和 `/api/v1/online-map/**` 时，路径前缀、认证方式、响应格式、错误码、请求编号、审计摘要和测试口径必须保持原样。

本阶段不退役 `api-gateway-service:8125`，不退役 `business-core-service:8130`，不退役 `admission-core-service:8131`，不退役 `engagement-core-service:8132`，不退役 `ops-core-service:8133`，不退役 `portal-core-service:8134`，不修改 25 条网关业务路由的现有上游配置，不修改前端，不恢复任何旧服务目录，不把外部节点执行器放进候选入口进程，不把业务路径改写成 `/api/v1/unified-backend/auth`、`/api/v1/unified-backend/onboarding`、`/api/v1/unified-backend/community`、`/api/v1/unified-backend/ops-control` 或 `/api/v1/unified-backend/guides` 之类新前缀。

`ops-core` 自有接口和七个继承业务路由在本阶段必须标记为 `IN_PROCESS`。外部节点执行器已出仓，必须以 `EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 和 `EXTERNAL_EXECUTOR_NOT_CONNECTED` 记录状态，不得进入候选入口依赖、源码扫描、编译 source root 或 component scan。

## 基础路径、端口和认证

`unified-backend-service` 本地端口固定为 `8135`。自有接口使用 `/api/v1/unified-backend` 前缀。

健康检查无需认证。后台自有接口要求 `Authorization: Bearer <token>`。本地契约允许 `helper-token`、`admin-token` 和 `owner-token` 读取摘要、挂载清单和 readiness；HTTP smoke 只允许 `admin-token` 和 `owner-token`。`user-token` 返回 `42001`，缺失 token 返回 `41000`，非 Bearer 返回 `41003`。

候选入口接受现有网关可信上下文时，仍沿用 `X-Gateway-Internal-Request-Id`、`X-Gateway-Internal-Timestamp`、`X-Gateway-Internal-Signature` 和 `X-Beiming-Actor-*` 的现有口径。浏览器伪造可信头不得覆盖真实身份。

## 运行画像字段

统一后端候选摘要必须固定暴露以下字段：`service=unified-backend`、`deploymentMode=CANDIDATE_PARALLEL_ENTRYPOINT`、`candidatePort=8135`、`currentProductionEntrypointsTotal=6`、`candidateEntrypointsTotal=1`、`mountedEntrypoints=["api-gateway","business-core","admission-core","engagement-core","ops-core","portal-core"]`、`mountedRouteIds=["auth","profile","notification","content","server-status","resource","admin","onboarding","exam","whitelist","attendance","community","activity","calendar","changelog","ops-control","cloudreve-sync","backup-recovery","alerting","plugin-integration","cross-platform-notification","ops-image-market","guide","material","online-map"]`、`inProcessRoutesTotal=25`、`httpFallbackRoutesTotal=0`、`externalRoutesTotal=0`、`externalNodeExecutorOutOfRepository=true`、`externalNodeExecutorConnected=false`、`externalNodeExecutorProject=separate-project`、`readyToReplaceGateway=false`、`readyToRetireBusinessCore=false`、`readyToRetireAdmissionCore=false`、`readyToRetireEngagementCore=false`、`readyToRetireOpsCore=false` 和 `readyToRetirePortalCore=false`。

挂载状态枚举固定为 `IN_PROCESS` 和 `HTTP_UPSTREAM_FALLBACK`。`IN_PROCESS` 表示请求在候选入口内由本地控制器处理，不调用 `GatewayHttpClient` 转发到当前 core 上游。`HTTP_UPSTREAM_FALLBACK` 表示仍沿用当前网关 HTTP 代理模式，本阶段除显式降级演练外不得出现在挂载清单里。外部节点执行器不再作为挂载项返回。

## 接口总览

| 接口 | 方法 | 路径 | 认证 | 权限 | 风险 |
| --- | --- | --- | --- | --- | --- |
| 候选健康检查 | GET | `/api/v1/unified-backend/health` | 否 | 无 | LOW |
| 候选摘要 | GET | `/api/v1/unified-backend/admin/ops/summary` | 是 | `HELPER`、`ADMIN` 或 `OWNER` | LOW |
| 候选挂载清单 | GET | `/api/v1/unified-backend/admin/mounts` | 是 | `HELPER`、`ADMIN` 或 `OWNER` | LOW |
| 候选 readiness | GET | `/api/v1/unified-backend/admin/readiness` | 是 | `HELPER`、`ADMIN` 或 `OWNER` | LOW |
| 候选 HTTP smoke | POST | `/api/v1/unified-backend/admin/http-smoke/run` | 是 | `ADMIN` 或 `OWNER` | LOW |

## 候选健康检查

`GET /api/v1/unified-backend/health`

成功响应 HTTP `200`。`data` 必须包含 `service=unified-backend`、`status=UP`、`port=8135`、`deploymentMode=CANDIDATE_PARALLEL_ENTRYPOINT`、`mountedEntrypoints`、`mountedRouteIds` 和 `generatedAt`。该接口只表示候选进程存活，不探测上游，不代表可替换当前网关。

## 候选摘要

`GET /api/v1/unified-backend/admin/ops/summary`

成功响应 HTTP `200`。`data` 必须包含运行画像字段、`gatewayApiMounted=true`、`businessCoreMounted=true`、`admissionCoreMounted=true`、`engagementCoreMounted=true`、`opsCoreMounted=true`、`portalCoreMounted=true`、`productionEntrypointsPreserved=true`、`legacyEntrypointsRestored=false`、`productionGaps` 和 `generatedAt`。`productionGaps` 必须至少包含尚未替换当前网关、尚未退役 business-core、尚未退役 admission-core、尚未退役 engagement-core、尚未退役 ops-core、尚未退役 portal-core、动态服务发现未接入、集中配置未接入、持久化审计未接入、真实生产流量演练未完成和外部节点执行器未连接。

## 候选挂载清单

`GET /api/v1/unified-backend/admin/mounts`

成功响应 HTTP `200`。`data.items` 必须列出 25 条网关业务路由以及 `api-gateway`、`business-core`、`admission-core`、`engagement-core`、`ops-core`、`portal-core` 和 `unified-backend` 自有入口。25 条业务路由和六个当前 core 自有入口的 `mountDisposition` 必须为 `IN_PROCESS`。挂载清单不得出现 `HTTP_UPSTREAM_FALLBACK`，除非后续契约明确进入降级回退演练。挂载清单不得出现外部节点执行器路由。

每项至少包含 `routeId`、`serviceKey`、`pathPrefix`、`sourceEntrypoint`、`candidateEntrypoint`、`mountDisposition`、`currentPort`、`candidatePort`、`preservesPathPrefix`、`preservesAuth`、`preservesResponseEnvelope` 和 `boundaryReason`。挂载清单不得返回 token、Cookie、内部签名、节点密钥、异常栈或本地用户目录。

## 候选 readiness

`GET /api/v1/unified-backend/admin/readiness`

成功响应 HTTP `200`。`data` 必须包含 `readyForProduction=false`、`readyToReplaceGateway=false`、`readyToRetireBusinessCore=false`、`readyToRetireAdmissionCore=false`、`readyToRetireEngagementCore=false`、`readyToRetireOpsCore=false`、`readyToRetirePortalCore=false`、`currentProductionEntrypointsTotal=6`、`candidateEntrypointsTotal=1`、`checks`、`lastHttpSmokeStatus`、`productionBlockers`、`productionSwitchReadinessStatus`、`productionSwitchChecks`、`centralConfigPrecheckStatus`、`centralConfigPrecheckChecks`、`centralConfigGovernancePrecheckStatus`、`centralConfigGovernancePrecheckChecks`、`centralConfigGovernanceEvidence`、`productionCentralConfigPrecheckStatus`、`productionCentralConfigPrecheckChecks`、`productionCentralConfigEvidence`、`productionCentralConfigProviderStatus`、`productionCentralConfigProviderChecks`、`productionCentralConfigProviderEvidence`、`externalEntrypointCutoverPrecheckStatus`、`externalEntrypointCutoverPrecheckChecks`、`externalEntrypointCutoverEvidence`、`externalEntrypointConfigSamplePrecheckStatus`、`externalEntrypointConfigSamplePrecheckChecks`、`externalEntrypointConfigSampleEvidence`、`externalEntrypointLocalCutoverRehearsalStatus`、`externalEntrypointLocalCutoverRehearsalChecks`、`externalEntrypointLocalCutoverRehearsalEvidence`、`productionCutoverRunbookStatus`、`productionCutoverRunbookChecks`、`productionCutoverRunbookEvidence`、`productionCutoverApprovalPackageStatus`、`productionCutoverApprovalPackageChecks`、`productionCutoverApprovalPackageEvidence`、`productionCutoverExternalParameterManifestStatus`、`productionCutoverExternalParameterManifestChecks`、`productionCutoverExternalParameterManifestEvidence`、`productionCutoverEvidenceConsistencyAuditStatus`、`productionCutoverEvidenceConsistencyAuditChecks`、`productionCutoverEvidenceConsistencyAuditEvidence`、`productionRuntimeConfigShellStatus`、`productionRuntimeConfigShellChecks`、`productionRuntimeConfigShellEvidence`、`productionAuditObservabilitySmokeStatus`、`productionAuditObservabilitySmokeChecks`、`productionAuditObservabilitySmokeEvidence`、`productionControlledCutoverStatus`、`productionControlledCutoverChecks`、`productionControlledCutoverEvidence`、`apiGatewayControlledRetirementStatus`、`apiGatewayControlledRetirementChecks`、`apiGatewayControlledRetirementEvidence`、`productionExternalValueIntakeRehearsalStatus`、`productionExternalValueIntakeRehearsalChecks`、`productionExternalValueIntakeRehearsalEvidence`、`auditSinkAdapterRehearsalStatus`、`auditSinkAdapterRehearsalChecks`、`auditSinkAdapterRehearsalEvidence`、`productionAuditSinkPrecheckStatus`、`productionAuditSinkPrecheckChecks`、`productionAuditSinkEvidence`、`persistentAuditPrecheckStatus`、`persistentAuditPrecheckChecks`、`persistentAuditGovernancePrecheckStatus`、`persistentAuditGovernancePrecheckChecks`、`persistentAuditGovernanceEvidence`、`realHttpRehearsalPrecheckStatus`、`realHttpRehearsalPrecheckChecks`、`routeDriftPrecheckStatus`、`routeDriftPrecheckChecks`、`rollbackWindowPrecheckStatus`、`rollbackWindowPrecheckChecks`、`rollbackWindowEvidence`、`entrypointSwitchPrecheckStatus`、`entrypointSwitchPrecheckChecks`、`entrypointSwitchEvidence`、`productionTrafficCanaryEvidence`、`backendSingleServicePrecheckStatus`、`backendSingleServicePrecheckChecks`、`backendSingleServiceEvidence`、`finalBackendSingleServicePrecheckStatus`、`finalBackendSingleServicePrecheckChecks`、`finalBackendSingleServiceEvidence`、`singleServiceCutoverPrecheckStatus`、`singleServiceCutoverPrecheckChecks`、`singleServiceCutoverEvidence`、`entrypointCutoverAdapterPrecheckStatus`、`entrypointCutoverAdapterPrecheckChecks`、`entrypointCutoverAdapterEvidence`、`oldEntrypointRetirementPrecheckStatus`、`oldEntrypointRetirementPrecheckChecks`、`oldEntrypointRetirementEvidence`、`entrypointCutoverExecutionPrecheckStatus`、`entrypointCutoverExecutionPrecheckChecks`、`entrypointCutoverExecutionEvidence`、`productionEntrypointCutoverPrecheckStatus`、`productionEntrypointCutoverPrecheckChecks`、`productionEntrypointCutoverEvidence`、`apiGatewayRetirementPrecheckStatus`、`apiGatewayRetirementPrecheckChecks`、`apiGatewayRetirementEvidence`、`coreEntrypointRetirementPrecheckStatus`、`coreEntrypointRetirementPrecheckChecks`、`coreEntrypointRetirementEvidence`、`productionHardeningPrecheckStatus`、`productionHardeningPrecheckChecks`、`productionHardeningEvidence`、`replacementDecision` 和 `generatedAt`。

本阶段即便所有测试通过，readiness 也不能声明可替换当前入口。`checks` 必须把 `api-gateway` 自有 API 挂载、`business-core` 自有 API 挂载、`admission-core` 自有 API 挂载、`engagement-core` 自有 API 挂载、`ops-core` 自有 API 挂载、`portal-core` 自有 API 挂载、第一批七个业务路由 in-process、第二批四个入服准入路由 in-process、第三批四个社区运营路由 in-process、第四批和第六期七个运维通知路由 in-process、第五批三个门户体验路由 in-process、旧入口保留、外部节点执行器出仓、路径前缀保留和响应格式保留列为通过或待验证项；动态服务发现未接入、集中配置未接入、生产审计未接入和真实生产流量演练未完成必须保留为阻塞。

为后续最终合并成一个后端应用服务做准备，readiness 必须额外暴露生产切换检查矩阵。`productionSwitchReadinessStatus` 在本阶段固定为 `BLOCKED`。`productionSwitchChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `ALL_CURRENT_BUSINESS_ROUTES_IN_PROCESS`、`CURRENT_ENTRYPOINTS_PRESERVED`、`ROUTE_PREFIX_AND_RESPONSE_PRESERVED`、`EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 和 `LEGACY_ENTRYPOINTS_NOT_RESTORED` 必须为 `PASS`；`CENTRAL_CONFIG_READY`、`PERSISTENT_AUDIT_READY`、`REAL_HTTP_SMOKE_REHEARSAL_READY`、`FRONTEND_ENTRYPOINT_SWITCH_READY`、`ROLLBACK_WINDOW_READY` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须为 `BLOCKED`。`replacementDecision` 必须包含 `canReplaceGateway=false`、`canRetireIndependentCoreEntrypoints=false`、`canRetireApiGateway=false`、`externalNodeExecutorOutOfRepository=true`、`externalNodeExecutorConnected=false`、`candidateCoverageStatus=PASS` 和 `reason`。该矩阵只用于生产切换准备，不得把外部节点执行器误报为已合并或已接入，也不得把当前候选入口描述为生产替换完成。

为避免后续切换单服务时配置漂移，readiness 必须额外暴露集中配置预检摘要。`centralConfigPrecheckStatus` 在本阶段固定为 `BLOCKED`。`centralConfigPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CANDIDATE_PORT_FIXED`、`CURRENT_ENTRYPOINT_PORTS_DOCUMENTED`、`IN_PROCESS_ROUTE_REGISTRY_FIXED`、`EXTERNAL_NODE_EXECUTOR_CONFIG_BOUNDARY_DOCUMENTED`、`DANGEROUS_TEST_CONTROLS_DISABLED`、`CONFIG_DRIFT_SCAN_AUTOMATED` 和 `CONFIG_ROLLBACK_SOURCE_DEFINED` 必须为 `PASS`；`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND` 和 `SENSITIVE_CONFIG_SOURCE_EXTERNALIZED` 必须为 `BLOCKED`。集中配置预检只判断候选入口是否具备进入生产配置治理的基础，不得读取或返回真实密钥、token、Cookie、内部签名明文、本地用户目录或完整环境变量。

为第十九轮集中配置治理做准备，readiness 必须额外暴露集中配置治理预检摘要。`centralConfigGovernancePrecheckStatus` 在本阶段固定为 `BLOCKED`，只表示仓库内已经形成配置归属、端口、候选配置面、漂移扫描、回滚来源和脱敏守卫证据，不表示真实集中配置提供方、生产 profile 或敏感配置来源已经接入。`centralConfigGovernancePrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CONFIG_OWNERSHIP_DOCUMENTED`、`ENTRYPOINT_PORTS_DOCUMENTED`、`CANDIDATE_CONFIG_SURFACE_DOCUMENTED`、`CONFIG_DRIFT_SCAN_AUTOMATED`、`CONFIG_ROLLBACK_SOURCE_DEFINED`、`SENSITIVE_VALUE_REDACTION_ENFORCED`、`EXTERNAL_NODE_EXECUTOR_CONFIG_BOUNDARY_PRESERVED` 和 `CONFIG_GOVERNANCE_EVIDENCE_RECORDED` 必须为 `PASS`；`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须为 `BLOCKED`。`centralConfigGovernanceEvidence` 必须包含 `governanceMode=DOCUMENTED_NOT_CONNECTED`、`candidateEntrypoint=unified-backend:8135`、`currentEntrypointPorts`、`configProviderStatus=BLOCKED`、`productionProfileBound=false`、`sensitiveValuesExternalized=false`、`configDriftScanAutomated=true`、`rollbackSourceDefined=true`、`sensitiveValuesExposed=false`、`environmentVariablesRead=false`、`trafficSwitchApplied=false`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`externalNodeExecutorOutOfRepository=true` 和 `status=GOVERNANCE_EVIDENCE_RECORDED_NOT_CONNECTED`。该 evidence 不得读取或返回真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令或真实文件路径。

为第二十八轮集中配置生产前置闭环做准备，readiness 必须额外暴露生产集中配置前置摘要。`productionCentralConfigPrecheckStatus` 在真实集中配置提供方未连接时固定为 `BLOCKED_BY_PRODUCTION_CONFIG_PROVIDER_NOT_CONNECTED`，只表示仓库内已经形成进入生产配置接入前的可验证证据，不表示真实 provider、生产 profile、敏感配置源、前端入口、外部代理或生产流量已经切换。`productionCentralConfigPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CONFIG_OWNERSHIP_DOCUMENTED`、`ENTRYPOINT_PORTS_DOCUMENTED`、`CANDIDATE_CONFIG_SURFACE_DOCUMENTED`、`CONFIG_DRIFT_SCAN_AUTOMATED`、`CONFIG_ROLLBACK_SOURCE_DEFINED`、`SENSITIVE_VALUE_REDACTION_ENFORCED`、`ROLLBACK_ENTRYPOINTS_DOCUMENTED` 和 `CURRENT_GATEWAY_ENTRYPOINT_PRESERVED` 必须为 `PASS`；`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须为 `BLOCKED`。`productionCentralConfigEvidence` 必须包含 `readinessMode=PRODUCTION_PREREQUISITES_RECORDED_NOT_CONNECTED`、`candidateEntrypoint=unified-backend:8135`、`currentEntrypoint=api-gateway:8125`、`rollbackEntrypoints`、`configDomains`、`configProviderStatus=BLOCKED`、`productionProfileBound=false`、`sensitiveConfigExternalized=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`configDriftScanAutomated=true`、`rollbackSourceDefined=true`、`trafficSwitchApplied=false`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`productionTrafficEntrypointReady=false`、`currentEntrypointPreserved=true` 和 `status=BLOCKED_BY_PRODUCTION_CONFIG_PROVIDER_NOT_CONNECTED`。该 evidence 不得读取或返回真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、数据库连接串、节点密钥、真实终端命令或真实文件路径。

为第二十九轮统一后端真实入口切换与观测闭环做准备，readiness 必须额外暴露外部入口切换适配与观测门禁。`externalEntrypointCutoverPrecheckStatus` 在仓库未提供真实外部入口配置时固定为 `BLOCKED_BY_EXTERNAL_ENTRYPOINT_CONFIG_NOT_PROVIDED`，只表示候选入口已经具备外部切流前的配置适配、观测条件、回滚条件和脱敏证据，不表示前端 base URL、反向代理 upstream、部署入口或生产流量已经切到 `unified-backend:8135`。`externalEntrypointCutoverPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_TARGET_READY`、`BUSINESS_PATHS_PRESERVED`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED`、`ROLLBACK_TARGET_DEFINED` 和 `SMOKE_EVIDENCE_FORMAT_DEFINED` 必须为 `PASS`；`EXTERNAL_ENTRYPOINT_CONFIG_PROVIDED`、`EXTERNAL_ENTRYPOINT_TARGETS_UNIFIED_BACKEND`、`CONTROLLED_CUTOVER_WINDOW_APPROVED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PERSISTENT_AUDIT_SINK_CONNECTED` 和 `USER_RETIREMENT_APPROVAL_GRANTED` 必须为 `BLOCKED`。`externalEntrypointCutoverEvidence` 必须包含 `readinessMode=EXTERNAL_CUTOVER_ADAPTER_RECORDED_NOT_SWITCHED`、`candidateEntrypoint=unified-backend:8135`、`currentEntrypoint=api-gateway:8125`、`effectiveEntrypoint=api-gateway:8125`、`rollbackEntrypoint=api-gateway:8125`、`businessPathsRemainUnchanged=true`、`externalEntrypointConfigProvided=false`、`externalEntrypointTargetsUnifiedBackend=false`、`repositoryCutoverConfigApplied=false`、`controlledCutoverWindowApproved=false`、`trafficSwitchApplied=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`centralConfigProviderConnected=false`、`persistentAuditSinkConnected=false`、`apiGatewayRetirementApproved=false`、`coreRetirementApproved=false`、`deletionAllowed=false`、`readyForProduction=false`、`readyToReplaceGateway=false` 和 `status=BLOCKED_BY_EXTERNAL_ENTRYPOINT_CONFIG_NOT_PROVIDED`。该门禁不读取真实代理配置，不读取环境变量，不发起外部 HTTP 探测，不调用部署平台，不写真实审计，不伪造生产流量、回滚窗口、流量归零或退役审批结果，不返回 token、Cookie、内部签名、本地用户目录、完整环境变量、数据库连接串、节点密钥、真实终端命令或真实文件路径。

为第三十一轮外部入口切流配置样板做准备，readiness 必须额外暴露样板配置门禁。`externalEntrypointConfigSamplePrecheckStatus` 在仓库已经提供可提交样板但样板尚未应用时固定为 `BLOCKED_BY_CUTOVER_SAMPLE_NOT_APPLIED`，只表示仓库内已经形成外部入口切流样板、配置路径、脱敏守卫和回滚口径，不表示样板已经应用到生产入口，也不表示生产流量、前端入口或代理 upstream 已经切到 `unified-backend:8135`。`externalEntrypointConfigSamplePrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CUTOVER_SAMPLE_PRESENT`、`CUTOVER_SAMPLE_JSON_PARSABLE`、`CANDIDATE_ENTRYPOINT_RECORDED`、`CURRENT_ENTRYPOINT_RECORDED`、`ROLLBACK_ENTRYPOINT_RECORDED`、`BUSINESS_PATHS_PRESERVED`、`SMOKE_TARGETS_RECORDED`、`NO_SENSITIVE_VALUES_IN_SAMPLE`、`PRODUCTION_SWITCH_DEFAULT_FALSE` 和 `API_GATEWAY_ROLLBACK_PROTECTED` 必须为 `PASS`。`externalEntrypointConfigSampleEvidence` 必须包含 `sampleConfigPath=docs/deployment-entrypoint-cutover-sample.json`、`sampleConfigPresent=true`、`sampleConfigApplied=false`、`applyProductionTraffic=false`、`requiresUserApprovalBeforeApply=true`、`businessPathRewriteAllowed=false`、`smokeTargetsTotal=32`、`currentEntrypoint=http://127.0.0.1:8125`、`candidateEntrypoint=http://127.0.0.1:8135`、`rollbackEntrypoint=http://127.0.0.1:8125`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false` 和 `status=BLOCKED_BY_CUTOVER_SAMPLE_NOT_APPLIED`。该门禁只允许记录样板与回滚口径，不得返回真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令、真实文件路径或任何生产切流结果。

为第三十二轮外部入口切流本地演练执行层做准备，readiness 必须额外暴露本地演练结果。`externalEntrypointLocalCutoverRehearsalStatus` 在本地样板演练通过但没有生产切流时固定为 `PASS_LOCAL_REHEARSAL_NOT_PRODUCTION`，只表示候选入口、当前入口、回滚入口、32 个 smoke 目标、路径保持、默认不切生产和脱敏规则已经由本地自动化验证，不表示前端 base URL、外部代理 upstream、部署入口或生产流量已经切到 `unified-backend:8135`。`externalEntrypointLocalCutoverRehearsalChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CUTOVER_SAMPLE_LOADED`、`CUTOVER_SAMPLE_JSON_PARSABLE`、`CANDIDATE_ENTRYPOINT_MATCHES_UNIFIED_BACKEND`、`CURRENT_ENTRYPOINT_MATCHES_API_GATEWAY`、`ROLLBACK_ENTRYPOINT_MATCHES_API_GATEWAY`、`SMOKE_TARGETS_COMPLETE`、`BUSINESS_PATHS_PRESERVED`、`PRODUCTION_TRAFFIC_SWITCH_REMAINS_FALSE`、`ROLLBACK_TARGET_PROTECTED`、`LOCAL_REHEARSAL_EXECUTED`、`NO_SENSITIVE_VALUES_IN_REHEARSAL` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`。`externalEntrypointLocalCutoverRehearsalEvidence` 必须包含 `readinessMode=LOCAL_EXTERNAL_ENTRYPOINT_CUTOVER_REHEARSAL_EXECUTED_NOT_PRODUCTION`、`sampleConfigPath=docs/deployment-entrypoint-cutover-sample.json`、`sampleConfigPresent=true`、`sampleConfigApplied=false`、`localRehearsalExecuted=true`、`applyProductionTraffic=false`、`currentEntrypoint=http://127.0.0.1:8125`、`candidateEntrypoint=http://127.0.0.1:8135`、`rollbackEntrypoint=http://127.0.0.1:8125`、`smokeTargetsTotal=32`、`businessPathsRemainUnchanged=true`、`businessPathRewriteAllowed=false`、`sensitiveValuesExposed=false`、`productionTrafficObserved=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`EXTERNAL_PROXY_CONFIG_APPLIED`、`FRONTEND_ENTRYPOINT_SWITCH_APPLIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PERSISTENT_AUDIT_SINK_CONNECTED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。该 evidence 不得读取真实环境变量，不得返回真实外部代理配置，不得调用部署平台，不得写真实审计，不得伪造生产流量、回滚窗口或流量归零结果，不得返回 token、Cookie、内部签名、本地用户目录、完整环境变量、数据库连接串、节点密钥、真实终端命令或真实文件路径。

为第三十三轮集中配置 provider 接入骨架做准备，readiness 必须额外暴露本地 file provider 演练结果。`productionCentralConfigProviderStatus` 在本地非密钥样板可加载、可解析、可校验且真实生产 provider 未连接时固定为 `PASS_LOCAL_FILE_PROVIDER_REHEARSAL_NOT_PRODUCTION`，只表示候选入口已经具备可替换 provider 边界和本地样板映射，不表示 Nacos、Spring Cloud Config、Consul、Kubernetes ConfigMap、部署平台配置、生产 profile 或真实敏感配置源已经接入。`productionCentralConfigProviderChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `LOCAL_CONFIG_PROVIDER_ABSTRACTION_CREATED`、`LOCAL_CONFIG_SAMPLE_PRESENT`、`LOCAL_CONFIG_SAMPLE_JSON_PARSABLE`、`CONFIG_DOMAINS_DECLARED`、`ENTRYPOINT_CONFIG_MATCHES_CUTOVER_SAMPLE`、`ROLLBACK_CONFIG_PRESERVED`、`BUSINESS_PATH_POLICY_PRESERVED`、`CONFIG_REDACTION_RULES_ENFORCED` 和 `CONFIG_DRIFT_SCAN_REUSES_PROVIDER_SNAPSHOT` 必须为 `PASS`；`PRODUCTION_PROVIDER_NOT_CONNECTED`、`PRODUCTION_PROFILE_NOT_BOUND` 和 `READY_FLAGS_REMAIN_FALSE` 必须继续表达真实生产接入未完成，其中前两项状态为 `BLOCKED`，最后一项为 `PASS`。`productionCentralConfigProviderEvidence` 必须包含 `readinessMode=LOCAL_FILE_CONFIG_PROVIDER_REHEARSAL_NOT_PRODUCTION`、`providerType=LOCAL_FILE_SAMPLE`、`providerConnected=false`、`sampleConfigPath=docs/unified-backend-central-config-provider-sample.json`、`sampleConfigPresent=true`、`sampleConfigParsed=true`、`configDomainsTotal=8`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`businessPathsRemainUnchanged=true`、`businessPathRewriteAllowed=false`、`productionProfileBound=false`、`sensitiveConfigExternalized=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`trafficSwitchApplied=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_FILE_PROVIDER_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`PERSISTENT_AUDIT_SINK_CONNECTED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。样板文件只允许保存非密钥、非生产、默认不生效的配置域摘要，不得提交真实 provider 地址、真实域名、token、Cookie、内部签名、数据库连接串、本地用户目录、节点密钥、真实终端命令、真实部署平台配置或真实生产凭据。

为第三十轮真实持久化审计接入前置闭环做准备，readiness 必须额外暴露生产 audit sink 接入前置门禁。`productionAuditSinkPrecheckStatus` 在仓库没有真实 audit sink 配置时固定为 `BLOCKED_BY_PERSISTENT_AUDIT_SINK_NOT_CONFIGURED`，只表示仓库内已经形成真实持久化审计接入前的契约、配置键、写入路径、失败降级、回放范围、保留策略、导出路径、脱敏规则、回滚来源、smoke 口径和生产流量审计观测门禁，不表示真实生产 audit sink 已连接，不表示真实审计事件已写入生产存储，不表示真实回放、保留或导出任务已执行，也不表示生产流量已经切到候选入口。`productionAuditSinkPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `AUDIT_EVENT_SCHEMA_FIXED`、`AUDIT_REQUEST_ID_PROPAGATED`、`AUDIT_ACTOR_FIELDS_DOCUMENTED`、`AUDIT_TARGET_FIELDS_DOCUMENTED`、`AUDIT_RESULT_FIELDS_DOCUMENTED`、`AUDIT_REDACTION_RULES_ENFORCED`、`AUDIT_FAILURE_DEGRADATION_DEFINED`、`AUDIT_REPLAY_SCOPE_DEFINED`、`AUDIT_RETENTION_POLICY_DEFINED` 和 `AUDIT_ROLLBACK_SOURCE_DEFINED` 必须为 `PASS`；`PERSISTENT_AUDIT_SINK_CONFIGURED`、`AUDIT_WRITE_PATH_CONNECTED`、`AUDIT_WRITE_SMOKE_PASSED`、`AUDIT_REPLAY_PATH_CONNECTED`、`AUDIT_RETENTION_JOB_CONNECTED`、`AUDIT_EXPORT_PATH_CONNECTED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`EXTERNAL_ENTRYPOINT_TARGETS_UNIFIED_BACKEND` 和 `PRODUCTION_AUDIT_TRAFFIC_OBSERVED` 必须为 `BLOCKED`。`productionAuditSinkEvidence` 必须包含 `readinessMode=PRODUCTION_AUDIT_SINK_CONTRACT_RECORDED_NOT_CONNECTED`、`candidateEntrypoint=unified-backend:8135`、`currentEntrypoint=api-gateway:8125`、`auditSinkConfigured=false`、`auditWritePathConnected=false`、`auditWriteSmokePassed=false`、`auditReplayPathConnected=false`、`auditRetentionJobConnected=false`、`auditExportPathConnected=false`、`auditEventSchemaFixed=true`、`requestIdPropagated=true`、`actorFieldsDocumented=true`、`targetFieldsDocumented=true`、`resultFieldsDocumented=true`、`failureDegradationDefined=true`、`redactionRulesEnforced=true`、`replayScopeDefined=true`、`retentionPolicyDefined=true`、`rollbackSourceDefined=true`、`centralConfigProviderConnected=false`、`externalEntrypointTargetsUnifiedBackend=false`、`productionAuditTrafficObserved=false`、`sensitiveValuesExposed=false`、`environmentVariablesRead=false`、`trafficSwitchApplied=false`、`readyForProduction=false`、`readyToReplaceGateway=false` 和 `status=BLOCKED_BY_PERSISTENT_AUDIT_SINK_NOT_CONFIGURED`。该门禁不读取真实 audit sink URL、DSN、topic、bucket、数据库连接串、账号、密码、token、Cookie、内部签名、节点密钥、完整环境变量、本地用户目录、真实外部代理配置、真实终端命令或宿主绝对路径，不连接真实审计平台，不写真实审计事件，不执行真实保留清理，不执行真实导出，不退役旧入口。

生产审计事件最小 schema 在第三十轮固定为脱敏契约，不进入真实持久化实现。字段至少包含 `auditEventId`、`requestId`、`occurredAt`、`entrypoint`、`module`、`path`、`method`、`actorId`、`actorRole`、`capabilities`、`sourceIpHash`、`targetType`、`targetId`、`action`、`riskLevel`、`requestSummary`、`beforeStateSummary`、`afterStateSummary`、`result`、`failureReason` 和 `redactionApplied`。示例值只能使用脱敏占位，不得写入真实用户名、真实 IP、真实 token、真实代理、真实数据库或真实审计 sink 信息。

为第三十四轮持久化审计 sink 本地 adapter 演练做准备，readiness 必须额外暴露 `auditSinkAdapterRehearsalStatus`、`auditSinkAdapterRehearsalChecks` 和 `auditSinkAdapterRehearsalEvidence`。`auditSinkAdapterRehearsalStatus` 在本地非生产 JSONL 样板可加载、可解析、schema 可校验、写入 smoke 可演练、回放和导出摘要可演练且真实生产 sink 未连接时固定为 `PASS_LOCAL_AUDIT_SINK_REHEARSAL_NOT_PRODUCTION`，只表示候选入口具备可替换 audit sink adapter 骨架和本地演练证据，不表示真实数据库、对象存储、日志平台、审计平台、SIEM 或生产 audit sink 已经接入。`auditSinkAdapterRehearsalChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `LOCAL_AUDIT_SINK_ADAPTER_CREATED`、`AUDIT_SAMPLE_JSONL_PRESENT`、`AUDIT_SAMPLE_JSONL_PARSEABLE`、`AUDIT_EVENT_SCHEMA_DECLARED`、`AUDIT_EVENT_REQUIRED_FIELDS_PRESENT`、`AUDIT_REQUEST_ID_PROPAGATED`、`AUDIT_ACTOR_TARGET_ACTION_RECORDED`、`AUDIT_WRITE_SMOKE_REHEARSED`、`AUDIT_REPLAY_REHEARSED`、`AUDIT_EXPORT_SUMMARY_REHEARSED`、`AUDIT_RETENTION_POLICY_RECORDED`、`AUDIT_REDACTION_RULES_ENFORCED` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`；`PRODUCTION_AUDIT_SINK_NOT_CONNECTED` 和 `PRODUCTION_AUDIT_TRAFFIC_NOT_OBSERVED` 必须为 `BLOCKED`。`auditSinkAdapterRehearsalEvidence` 必须包含 `readinessMode=LOCAL_AUDIT_SINK_ADAPTER_REHEARSAL_NOT_PRODUCTION`、`sinkType=LOCAL_FILE_JSONL_SAMPLE`、`sinkConnected=false`、`sampleEventPath=docs/unified-backend-audit-sink-sample.jsonl`、`sampleSchemaPath=docs/unified-backend-audit-sink-sample-schema.json`、`sampleEventsPresent=true`、`sampleEventsParsed=true`、`sampleEventsTotal=4`、`writeSmokeRehearsed=true`、`replayRehearsed=true`、`exportSummaryRehearsed=true`、`retentionPolicyRecorded=true`、`auditEventSchemaVersion=1.0`、`requiredFieldsTotal`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`productionAuditSinkConnected=false`、`productionAuditTrafficObserved=false`、`trafficSwitchApplied=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_AUDIT_SINK_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_PERSISTENT_AUDIT_SINK_CONFIGURED`、`REAL_AUDIT_WRITE_PATH_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`REAL_AUDIT_REPLAY_PATH_CONNECTED`、`REAL_AUDIT_EXPORT_PATH_CONNECTED`、`REAL_AUDIT_RETENTION_JOB_CONNECTED`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。样板文件只允许保存非密钥、非生产、默认不生效的审计事件样板和 schema 摘要，不得包含真实用户、真实 IP、真实 token、真实 Cookie、真实数据库连接串、真实审计 sink 地址、本地用户目录、节点密钥、真实终端命令、真实部署平台配置或真实生产凭据。本地 adapter 不得读取真实环境变量，不得写真实运行文件，不得连接真实 sink，不得执行真实保留清理或真实导出。

为第三十五轮生产切换 runbook 与回滚窗口证据记录做准备，readiness 必须额外暴露 `productionCutoverRunbookStatus`、`productionCutoverRunbookChecks` 和 `productionCutoverRunbookEvidence`。`productionCutoverRunbookStatus` 在本地 runbook 样板可加载、可解析，候选入口、本地切流演练、本地 file provider、本地 audit sink adapter、32 个 smoke 目标、7 个 Maven 回归入口、灰度计划、观测字段、回滚命令、退役顺序和删除禁令都被统一记录且真实生产切流仍未执行时固定为 `PASS_LOCAL_CUTOVER_RUNBOOK_REHEARSAL_NOT_PRODUCTION`。该状态只表示生产切换前的操作手册和 evidence 面板已经本地闭环，不表示外部入口、前端 base URL、反向代理、部署入口、真实集中配置 provider、真实 audit sink 或生产流量已经切换。

`productionCutoverRunbookChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `RUNBOOK_SAMPLE_PRESENT`、`RUNBOOK_SAMPLE_JSON_PARSABLE`、`UNIFIED_BACKEND_CANDIDATE_READY`、`BUSINESS_PATHS_PRESERVED`、`ROUTE_DRIFT_SCAN_PASSED`、`LOCAL_ENTRYPOINT_REHEARSAL_PASSED`、`LOCAL_CONFIG_PROVIDER_REHEARSAL_PASSED`、`LOCAL_AUDIT_SINK_REHEARSAL_PASSED`、`SMOKE_TARGETS_RECORDED`、`ROLLBACK_COMMANDS_RECORDED`、`CANARY_PLAN_RECORDED`、`OBSERVATION_FIELDS_RECORDED`、`RETIREMENT_ORDER_RECORDED`、`NO_SENSITIVE_VALUES_IN_RUNBOOK` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`；`EXTERNAL_ENTRYPOINT_CONFIG_NOT_APPLIED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`ROLLBACK_WINDOW_NOT_STARTED` 和 `USER_RETIREMENT_APPROVAL_NOT_GRANTED` 必须为 `BLOCKED`。

`productionCutoverRunbookEvidence` 必须包含 `readinessMode=LOCAL_PRODUCTION_CUTOVER_RUNBOOK_REHEARSAL_NOT_PRODUCTION`、`sampleRunbookPath=docs/unified-backend-production-cutover-runbook-sample.json`、`sampleRunbookPresent=true`、`sampleRunbookParsed=true`、`sampleRunbookApplied=false`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`businessPathsRemainUnchanged=true`、`smokeTargetsTotal=32`、`mavenEntrypointsTotal=7`、`rollbackCommandsRecorded=true`、`canaryPlanRecorded=true`、`observationFieldsRecorded=true`、`localConfigProviderRehearsalPassed=true`、`localAuditSinkRehearsalPassed=true`、`externalEntrypointConfigApplied=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowStarted=false`、`rollbackWindowCompleted=false`、`apiGatewayRetirementApproved=false`、`coreRetirementApproved=false`、`deletionAllowed=false`、`bulkRetirementAllowed=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_CUTOVER_RUNBOOK_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。

正式 runbook 样板文件为 `docs/unified-backend-production-cutover-runbook-sample.json`。样板必须是非生产、非密钥、默认不生效的 JSON 文件，固定 `mode=LOCAL_RUNBOOK_REHEARSAL_NOT_APPLIED`、`sampleApplied=false`、`productionTrafficAllowed=false` 和 `requiresUserApprovalBeforeApply=true`。样板不得包含真实域名、真实 token、真实 Cookie、真实数据库连接串、真实审计 sink 地址、真实集中配置地址、真实代理 upstream、真实部署平台地址、本地用户目录、节点密钥或真实终端命令。样板中的回滚、灰度、观测和退役内容只用于操作手册和本地 evidence，不得被解释为生产配置已经应用。

为避免后续切换单服务时审计链路漂移，readiness 必须额外暴露持久化审计预检摘要。`persistentAuditPrecheckStatus` 在本阶段固定为 `BLOCKED`。`persistentAuditPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `AUDIT_SINK_FIXED`、`AUDIT_REQUEST_ID_PRESERVED`、`AUDIT_EVENT_SCHEMA_FIXED`、`AUDIT_RETENTION_WINDOW_DOCUMENTED`、`AUDIT_BACKUP_EXPORT_PATH_DOCUMENTED` 和 `AUDIT_CONFIG_ROLLBACK_SOURCE_DEFINED` 必须为 `PASS`；`PERSISTENT_AUDIT_SINK_CONNECTED`、`AUDIT_WRITE_PATH_CONNECTED`、`AUDIT_REPLAY_PATH_CONNECTED` 和 `AUDIT_RETENTION_JOB_CONNECTED` 必须为 `BLOCKED`。持久化审计预检只判断候选入口是否具备稳定审计治理的前置条件，不得把模拟日志、内存队列、请求摘要或测试记录误报为真实持久化审计。

为第二十轮持久化审计治理做准备，readiness 必须额外暴露持久化审计治理预检摘要。`persistentAuditGovernancePrecheckStatus` 在本阶段固定为 `BLOCKED`，只表示仓库内已经形成审计归属、事件结构、请求编号、保留窗口、导出路径、回放范围、回滚来源和脱敏守卫证据，不表示真实持久化审计落点、写入路径、回放路径或保留任务已经接入。`persistentAuditGovernancePrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `AUDIT_OWNERSHIP_DOCUMENTED`、`AUDIT_EVENT_SCHEMA_DOCUMENTED`、`AUDIT_REQUEST_ID_PRESERVED`、`AUDIT_RETENTION_WINDOW_DOCUMENTED`、`AUDIT_EXPORT_PATH_DOCUMENTED`、`AUDIT_REPLAY_SCOPE_DOCUMENTED`、`AUDIT_CONFIG_ROLLBACK_SOURCE_DEFINED`、`AUDIT_REDACTION_ENFORCED`、`EXTERNAL_NODE_EXECUTOR_AUDIT_BOUNDARY_PRESERVED` 和 `PERSISTENT_AUDIT_GOVERNANCE_EVIDENCE_RECORDED` 必须为 `PASS`；`PERSISTENT_AUDIT_SINK_CONNECTED`、`AUDIT_WRITE_PATH_CONNECTED`、`AUDIT_REPLAY_PATH_CONNECTED`、`AUDIT_RETENTION_JOB_CONNECTED`、`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须为 `BLOCKED`。`persistentAuditGovernanceEvidence` 必须包含 `governanceMode=DOCUMENTED_NOT_CONNECTED`、`candidateEntrypoint=unified-backend:8135`、`auditSinkStatus=BLOCKED`、`auditWritePathConnected=false`、`auditReplayPathConnected=false`、`auditRetentionJobConnected=false`、`auditConfigRollbackSourceDefined=true`、`requestIdPreserved=true`、`eventSchemaDocumented=true`、`retentionWindowDocumented=true`、`exportPathDocumented=true`、`replayScopeDocumented=true`、`redactionEnforced=true`、`trafficSwitchApplied=false`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`externalNodeExecutorOutOfRepository=true` 和 `status=GOVERNANCE_EVIDENCE_RECORDED_NOT_CONNECTED`。该 evidence 不得返回真实外部代理配置、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令、真实文件路径或真实持久化审计内容。

为进入生产替换预演阶段，readiness 必须额外暴露真实 HTTP 演练预检摘要。第十二轮开始，候选入口必须有基于真实 Web 环境和 HTTP client 的自动化演练证据，不能只依赖 MockMvc、路由注册表或静态 smoke 目标。`realHttpRehearsalPrecheckStatus` 在回滚 runbook 和回滚复检完成前仍为 `BLOCKED`。`realHttpRehearsalPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CANDIDATE_HTTP_PORT_FIXED`、`REAL_HTTP_TARGETS_DOCUMENTED`、`AUTH_FAILURE_PATH_INCLUDED`、`EXTERNAL_NODE_EXECUTOR_EXCLUDED_FROM_REHEARSAL`、`SMOKE_RESULT_REDACTION_FIXED`、`CANDIDATE_PROCESS_STARTED_FOR_REHEARSAL`、`ALL_REAL_HTTP_TARGETS_PASSED` 和 `REHEARSAL_RESULT_RECORDED` 必须为 `PASS`；`REHEARSAL_RUNBOOK_DEFINED` 和 `REHEARSAL_ROLLBACK_RECHECKED` 必须为 `BLOCKED`。真实 HTTP 演练必须覆盖候选健康、六个入口健康、25 条业务路径和至少一个认证失败路径，并且结果不得泄露 Authorization、Cookie、凭据、异常栈、本地用户目录或完整环境变量。即便真实 HTTP 演练通过，`REAL_HTTP_SMOKE_REHEARSAL_READY` 仍必须保持 `BLOCKED`，直到回滚 runbook 和回滚复检也完成。

为避免生产入口切换时出现路由漂移，readiness 必须额外暴露路由漂移预检摘要。第十二轮开始，`routeDriftPrecheckStatus` 必须为 `PASS`，表示候选入口已经用自动化测试对比 `api-gateway` 当前 25 条业务路由和 `unified-backend` 挂载清单。`routeDriftPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CURRENT_GATEWAY_ROUTES_DOCUMENTED`、`UNIFIED_MOUNT_ROUTES_DOCUMENTED`、`ROUTE_PREFIX_PRESERVED`、`EXTERNAL_NODE_EXECUTOR_ROUTE_ABSENT`、`NO_HTTP_UPSTREAM_FALLBACK_IN_CANDIDATE`、`REAL_GATEWAY_TO_UNIFIED_DIFF_SCAN_AUTOMATED`、`AUTH_BEHAVIOR_DIFF_SCAN_AUTOMATED`、`ERROR_CODE_DIFF_SCAN_AUTOMATED`、`SENSITIVE_FIELD_DIFF_SCAN_AUTOMATED` 和 `DRIFT_SCAN_RESULT_RECORDED` 必须为 `PASS`。路由漂移预检只判断切流前的对比门槛，不新增业务路径，不改写任何原路径前缀，不把外部节点执行器纳入 in-process 候选。

为保留旧入口回退能力，readiness 必须额外暴露回滚窗口预检摘要。第十三轮开始，候选入口必须把回滚窗口时长、回滚触发条件、回滚复检自动化和回滚记录证据纳入只读 readiness 证据，但不得把旧入口退役审批误报为完成。`rollbackWindowPrecheckStatus` 在旧入口退役审批完成前仍为 `BLOCKED`。`rollbackWindowPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CURRENT_ENTRYPOINTS_STILL_PRESENT`、`CURRENT_ENTRYPOINT_TESTS_STILL_REQUIRED`、`API_GATEWAY_ROLLBACK_TARGET_DOCUMENTED`、`CORE_ENTRYPOINTS_ROLLBACK_TARGETS_DOCUMENTED`、`EXTERNAL_NODE_EXECUTOR_UNAFFECTED_BY_CANDIDATE`、`ROLLBACK_WINDOW_DURATION_DEFINED`、`ROLLBACK_TRIGGER_CRITERIA_DEFINED`、`ROLLBACK_RECHECK_AUTOMATED` 和 `ROLLBACK_RECORDING_COMPLETED` 必须为 `PASS`；`OLD_ENTRYPOINT_RETIREMENT_APPROVAL_READY` 必须为 `BLOCKED`。`rollbackWindowEvidence` 必须包含 `windowDuration`、`triggerCriteria`、`recheckAutomation`、`rollbackTargets`、`recordingStatus` 和 `retirementApprovalStatus`。`windowDuration.status` 必须为 `DEFINED`，`windowDuration.minimumHours` 必须为 `24`；`triggerCriteria.items` 必须至少覆盖真实 HTTP 演练失败、路由漂移、认证错误码漂移、当前入口回归失败、生产源码边界扫描命中和外部节点执行器边界异常；`recheckAutomation.commands` 必须记录候选入口测试、当前六个回滚入口回归、`git diff --check` 和生产源码边界扫描；`rollbackTargets` 必须记录 `api-gateway:8125`、五个 core 入口 `8130` 到 `8134` 和 `unified-backend:8135` 候选入口；`recordingStatus` 必须为 `COMPLETED`；`retirementApprovalStatus` 必须为 `BLOCKED`。回滚窗口预检不得把当前 6 个生产入口描述为可删除，`ROLLBACK_WINDOW_READY` 必须继续保持 `BLOCKED`。

为下一轮入口切换做准备，readiness 必须额外暴露入口切换开关画像。第十四轮开始，候选入口必须把入口切换演练目标、业务路径保持、测试自动化和审计记录就绪状态纳入只读 readiness evidence，但不得真实切换前端、外部代理或生产流量。第十五轮开始，候选入口必须把生产灰度流量计划纳入只读 readiness evidence，做法参考金丝雀发布的分阶权重、暂停门禁和回滚保留原则，但不得让候选入口接收真实生产流量。`entrypointSwitchPrecheckStatus` 在本阶段固定为 `BLOCKED`。`entrypointSwitchPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `BUSINESS_PATHS_REMAIN_UNCHANGED`、`CANDIDATE_BASE_URL_DOCUMENTED`、`FRONTEND_NOT_MODIFIED_IN_THIS_ROUND`、`PROXY_SWITCH_SCOPE_DOCUMENTED`、`SWITCH_REQUIRES_ROLLBACK_WINDOW`、`ENTRYPOINT_SWITCH_TESTS_AUTOMATED`、`SWITCH_AUDIT_RECORDING_READY` 和 `PRODUCTION_TRAFFIC_CANARY_DEFINED` 必须为 `PASS`；`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED` 和 `EXTERNAL_PROXY_SWITCH_IMPLEMENTED` 必须为 `BLOCKED`。`entrypointSwitchEvidence` 必须包含 `candidateBaseUrl=http://127.0.0.1:8135`、`currentGatewayBaseUrl=http://127.0.0.1:8125`、`businessPathsRemainUnchanged=true`、`switchMode=ENTRYPOINT_TARGET_ONLY`、`forbiddenPathPrefix=/api/v1/unified-backend/<module>`、`rollbackTarget=api-gateway:8125`、`rehearsalStatus=PASS` 和 `auditRecordingStatus=READY_FOR_REHEARSAL`。`productionTrafficCanaryEvidence` 必须包含 `strategy=CANARY_WITH_PAUSE_AND_ROLLBACK`、`plannedWeights=[0,5,25,50,100]`、`initialWeightPercent=0`、`currentProductionTrafficPercent=0`、`candidateProductionTrafficPercent=0`、`manualPromotionRequired=true`、`rollbackTarget=api-gateway:8125`、`rollbackWindowMinimumHours=24`、`trafficSwitchApplied=false`、`status=PLAN_DEFINED_NOT_APPLIED` 和 `gates`。`gates` 至少包含真实 HTTP 演练通过、路由漂移扫描通过、回滚窗口证据完成、当前入口全量回归通过、生产源码边界扫描无命中、前端入口开关就绪和外部代理开关就绪。入口切换开关画像只记录未来从 `8125` 切到 `8135` 的入口目标，不允许把业务路径改成 `/api/v1/unified-backend/<module>`，也不得返回真实外部代理配置、前端构建变量、token、Cookie、内部签名、本地用户目录或完整环境变量。`FRONTEND_ENTRYPOINT_SWITCH_READY` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须继续保持 `BLOCKED`。

为后端侧单服务生产入口准备，readiness 必须额外暴露后端单服务预检摘要。第十六轮开始，`backendSingleServicePrecheckStatus` 可以为 `PASS`，只表示 `unified-backend-service:8135` 已具备后端唯一候选入口的可运行、可配置、可审计、可回滚和可验证形态；它不得表示前端、外部代理或生产流量已经切换。`backendSingleServicePrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_COVERS_BACKEND_ENTRYPOINT_APIS`、`ALL_CURRENT_BUSINESS_ROUTES_IN_PROCESS`、`PATH_AUTH_ENVELOPE_AND_ERROR_CODES_PRESERVED`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED`、`SENSITIVE_FIELD_SCAN_PASSED`、`ROLLBACK_WINDOW_EVIDENCE_COMPLETED`、`CURRENT_ENTRYPOINTS_PRESERVED_AS_ROLLBACK`、`CURRENT_ENTRYPOINT_REGRESSION_REQUIRED`、`EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 和 `BACKEND_SINGLE_SERVICE_EVIDENCE_RECORDED` 必须为 `PASS`；`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED`、`OLD_ENTRYPOINT_RETIREMENT_APPROVED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED` 和 `PERSISTENT_AUDIT_SINK_CONNECTED` 必须为 `BLOCKED`。`backendSingleServiceEvidence` 必须包含 `candidateEntrypoint=unified-backend:8135`、`currentGatewayRollbackTarget=api-gateway:8125`、`currentCoreRollbackTargets=["business-core:8130","admission-core:8131","engagement-core:8132","ops-core:8133","portal-core:8134"]`、`externalNodeExecutorOutOfRepository=true`、`externalNodeExecutorConnected=false`、`businessPathsRemainUnchanged=true`、`inProcessRoutesTotal=25`、`httpFallbackRoutesTotal=0`、`currentProductionEntrypointsPreserved=true`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`trafficSwitchApplied=false`、`oldEntrypointRetirementApproved=false`、`backendSingleServiceCandidateReady=true` 和 `remainingBlockers`。`remainingBlockers` 至少包含前端入口未切换、外部代理未切换、生产流量未切换、旧入口退役审批未完成、集中配置未接入和持久化审计未接入。该 evidence 不得返回真实外部代理配置、前端构建变量、token、Cookie、内部签名、本地用户目录、环境变量、节点密钥、真实终端命令或真实文件路径。

为第二十一轮最终单服务后端侧收束，readiness 必须额外暴露最终单服务后端侧预检摘要。`finalBackendSingleServicePrecheckStatus` 只有在后端应用入口覆盖、25 条业务路由 in-process、真实 HTTP 演练、路由漂移扫描、旧入口回归、生产源码边界扫描、旧入口回滚保护和外部节点执行器出仓均完成自动化验证后才能为 `PASS`；即便为 `PASS`，也不得表示前端、外部代理、生产流量或旧入口退役已经完成。`finalBackendSingleServicePrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `BACKEND_APPLICATION_ENTRYPOINT_COVERAGE`、`ALL_CURRENT_BUSINESS_ROUTES_IN_PROCESS`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED`、`LEGACY_ENTRYPOINT_REGRESSION_PASSED`、`PRODUCTION_SOURCE_BOUNDARY_SCAN_PASSED`、`LEGACY_ROLLBACK_ENTRYPOINTS_PROTECTED`、`EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 和 `FINAL_BACKEND_SINGLE_SERVICE_EVIDENCE_RECORDED` 必须为 `PASS`；`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED`、`TRAFFIC_SWITCH_APPLIED`、`OLD_ENTRYPOINT_RETIREMENT_APPROVED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED` 和 `PERSISTENT_AUDIT_SINK_CONNECTED` 必须为 `BLOCKED`。`finalBackendSingleServiceEvidence` 必须包含 `targetBackendApplicationEntrypoint=unified-backend:8135`、`externalNodeExecutorOutOfRepository=true`、`externalNodeExecutorConnected=false`、`legacyRollbackEntrypoints`、`backendApplicationEntrypointsRequiredForFutureRuntime=["unified-backend:8135"]`、`businessPathsRemainUnchanged=true`、`inProcessRoutesTotal=25`、`httpFallbackRoutesTotal=0`、`currentProductionEntrypointsPreserved=true`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`trafficSwitchApplied=false`、`oldEntrypointRetirementApproved=false`、`singleBackendApplicationReadyForCutoverRehearsal=true` 和 `remainingBlockers`。`remainingBlockers` 至少包含前端入口未切换、外部代理未切换、生产流量未切换、旧入口退役审批未完成、集中配置未接入和持久化审计未接入。该 evidence 只能返回静态脱敏证据，不得读取环境变量，不得探测真实外部代理，不得启动外部进程，不得访问本地真实文件，不得调用外部节点执行器，不得返回 token、Cookie、内部签名、本地用户目录、节点密钥、真实终端命令或真实文件路径。

为第二十四轮官网后端单服务切换与出仓残留清理，readiness 必须额外暴露单服务切换预检摘要。`singleServiceCutoverPrecheckStatus` 可以为 `PASS_READY_FOR_EXTERNAL_CUTOVER`，只表示仓库内后端候选入口、节点执行器出仓残留清理、API 汇总同步、旧入口退役队列和自动化守卫已满足外部入口切换前置条件，不表示真实前端、外部代理、生产流量或旧入口退役已经完成。`singleServiceCutoverPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_TARGET_ENTRYPOINT_READY`、`ALL_OFFICIAL_BACKEND_ROUTES_IN_PROCESS`、`NODE_EXECUTOR_REPOSITORY_RESIDUALS_REMOVED`、`API_REFERENCE_SYNCHRONIZED` 和 `OLD_ENTRYPOINTS_IN_RETIREMENT_QUEUE` 必须为 `PASS`；`EXTERNAL_TRAFFIC_SWITCH_APPLIED` 和 `OLD_ENTRYPOINT_RETIREMENT_APPROVED` 必须为 `BLOCKED`。`singleServiceCutoverEvidence` 必须包含 `targetBackendApplicationEntrypoint=unified-backend:8135`、`officialBackendEntrypointsTotal=7`、`backendApplicationEntrypointsRequiredForFutureRuntime=["unified-backend:8135"]`、`rollbackEntrypoints=["api-gateway:8125","business-core:8130","admission-core:8131","engagement-core:8132","ops-core:8133","portal-core:8134"]`、`retirementQueue`、`businessPathsRemainUnchanged=true`、`inProcessRoutesTotal=25`、`httpFallbackRoutesTotal=0`、`externalRoutesTotal=0`、`nodeExecutorRepositoryResidualsRemoved=true`、`apiReferenceSynchronized=true`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false`、`trafficSwitchApplied=false`、`oldEntrypointRetirementApproved=false`、`readyForProduction=false`、`readyToReplaceGateway=false` 和 `status=READY_FOR_EXTERNAL_CUTOVER_NOT_SWITCHED`。该 evidence 不得返回外部节点执行器 API 路径、旧模块键、旧端口、真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令或真实文件路径。

为第十七轮不改页面的前端或代理入口切换做适配准备，readiness 必须额外暴露入口切换适配预检摘要。`entrypointCutoverAdapterPrecheckStatus` 在本阶段固定为 `BLOCKED`，只表示仓库内已形成从当前网关 `8125` 切到候选入口 `8135` 的适配口径和可验证证据，不表示前端工程、外部代理或生产流量已经切换。`entrypointCutoverAdapterPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `FRONTEND_API_BASE_URL_CONTRACT_DOCUMENTED`、`BUSINESS_PATHS_REMAIN_UNCHANGED`、`CANDIDATE_BASE_URL_DOCUMENTED`、`ROLLBACK_TARGET_DOCUMENTED`、`NO_FRONTEND_SOURCE_TO_MODIFY_IN_REPOSITORY`、`NO_PROXY_CONFIG_TO_MODIFY_IN_REPOSITORY`、`CUTOVER_REQUIRES_EXTERNAL_FRONTEND_OR_PROXY_CHANGE` 和 `CUTOVER_ADAPTER_EVIDENCE_RECORDED` 必须为 `PASS`；`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED` 和 `PRODUCTION_TRAFFIC_ENTRYPOINT_READY` 必须为 `BLOCKED`。`entrypointCutoverAdapterEvidence` 必须包含 `currentGatewayBaseUrl=http://127.0.0.1:8125`、`candidateBaseUrl=http://127.0.0.1:8135`、`switchMode=ENTRYPOINT_TARGET_ONLY`、`businessPathsRemainUnchanged=true`、`forbiddenPathPrefix=/api/v1/unified-backend/<module>`、`frontendSourcePresent=false`、`proxyConfigPresent=false`、`repositoryCutoverConfigApplied=false`、`requiredFrontendEnvVar=VITE_API_BASE_URL`、`recommendedNextValue=http://127.0.0.1:8135`、`rollbackTarget=http://127.0.0.1:8125`、`trafficSwitchApplied=false`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false` 和 `status=ADAPTER_EVIDENCE_RECORDED_NOT_APPLIED`。该 evidence 只允许记录切换适配口径，不得返回真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令或真实文件路径。

为第十八轮旧入口退役审批做准备，readiness 必须额外暴露旧入口退役预检摘要。`oldEntrypointRetirementPrecheckStatus` 在本阶段固定为 `BLOCKED`，只表示退役审批、退役顺序和回归守卫已经形成证据，不表示任何旧入口已经退役。`oldEntrypointRetirementPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `RETIREMENT_SCOPE_DOCUMENTED`、`SEQUENTIAL_ENTRYPOINT_RETIREMENT_REQUIRED`、`BULK_RETIREMENT_FORBIDDEN`、`CURRENT_ENTRYPOINT_REGRESSION_REQUIRED`、`ROLLBACK_TARGETS_STILL_PROTECTED`、`EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 和 `RETIREMENT_APPROVAL_EVIDENCE_RECORDED` 必须为 `PASS`；`FRONTEND_ENTRYPOINT_SWITCH_IMPLEMENTED`、`EXTERNAL_PROXY_SWITCH_IMPLEMENTED`、`PRODUCTION_TRAFFIC_ENTRYPOINT_READY`、`API_GATEWAY_RETIREMENT_APPROVED`、`CORE_ENTRYPOINT_RETIREMENT_APPROVED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED` 和 `PERSISTENT_AUDIT_SINK_CONNECTED` 必须为 `BLOCKED`。`oldEntrypointRetirementEvidence` 必须包含 `retirementMode=SEQUENTIAL_APPROVAL_ONLY`、`bulkRetirementAllowed=false`、`directoryDeletionAllowed=false`、`mavenRegressionRequired=true`、`externalNodeExecutorOutOfRepository=true`、`retirementApprovalStatus=BLOCKED`、`approvedEntrypoints=[]`、`currentProductionEntrypoints=["api-gateway:8125","business-core:8130","admission-core:8131","engagement-core:8132","ops-core:8133","portal-core:8134"]`、`protectedRollbackEntrypoints=["api-gateway:8125","business-core:8130","admission-core:8131","engagement-core:8132","ops-core:8133","portal-core:8134"]`、`blockedEntrypoints`、`nextEligibleEntrypoint=NONE_UNTIL_TRAFFIC_SWITCH`、`trafficSwitchApplied=false`、`frontendEntrypointSwitched=false`、`externalProxySwitched=false` 和 `status=RETIREMENT_APPROVAL_NOT_GRANTED`。该 evidence 不得返回真实外部代理配置、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令或真实文件路径，也不得把当前 6 个生产入口描述为可删除。

为第二十二轮统一后端入口切换执行演练做准备，readiness 必须额外暴露入口切换执行预检摘要。`entrypointCutoverExecutionPrecheckStatus` 在本阶段固定为 `BLOCKED`，只表示后端候选入口、路径保持、真实 HTTP 演练、路由漂移、旧入口回滚保护和外部节点执行器出仓已具备可验证证据，不表示本仓库已经切换前端、外部代理或生产流量。`entrypointCutoverExecutionPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `BUSINESS_PATHS_REMAIN_UNCHANGED`、`FORBIDDEN_UNIFIED_BUSINESS_PREFIX_ABSENT`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED`、`LEGACY_ROLLBACK_ENTRYPOINTS_PROTECTED`、`ROLLBACK_RECHECK_PASSED` 和 `EXTERNAL_NODE_EXECUTOR_OUT_OF_REPOSITORY` 必须为 `PASS`；`FRONTEND_OR_PROXY_CONFIG_PRESENT`、`FRONTEND_OR_PROXY_CONFIG_UPDATED`、`TARGET_ENTRYPOINT_SET_TO_UNIFIED_BACKEND`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`OLD_ENTRYPOINT_RETIREMENT_APPROVED`、`CENTRAL_CONFIG_PROVIDER_CONNECTED` 和 `PERSISTENT_AUDIT_SINK_CONNECTED` 必须为 `BLOCKED`。`entrypointCutoverExecutionEvidence` 必须包含 `currentGatewayBaseUrl=http://127.0.0.1:8125`、`candidateBaseUrl=http://127.0.0.1:8135`、`effectiveApiBaseUrl=http://127.0.0.1:8125`、`switchMode=ENTRYPOINT_TARGET_ONLY`、`businessPathsRemainUnchanged=true`、`forbiddenPathPrefix=/api/v1/unified-backend/<module>`、`frontendConfigPresent=false`、`proxyConfigPresent=false`、`repositoryCutoverConfigApplied=false`、`externalCutoverRequired=true`、`rollbackTarget=http://127.0.0.1:8125`、`trafficSwitchApplied=false`、`oldEntrypointRetirementApproved=false`、`externalNodeExecutorOutOfRepository=true`、`externalNodeExecutorConnected=false`、`readyToReplaceGateway=false`、`readyForProduction=false`、`status=CUTOVER_EXECUTION_BLOCKED_BY_EXTERNAL_ENTRYPOINT_CONFIG` 和 `remainingBlockers`。`remainingBlockers` 至少包含仓库缺少前端或代理配置、入口配置未改为 unified、生产流量未切换、旧入口退役审批未完成、集中配置未接入和持久化审计未接入。该 evidence 不得修改业务路径，不得真实切换外部入口，不得退役旧 Maven 入口，不得恢复旧服务目录，不得合并外部节点执行器，也不得返回真实外部代理配置、前端构建变量实际值、token、Cookie、内部签名、本地用户目录、完整环境变量、节点密钥、真实终端命令或真实文件路径。

为第二十五轮统一后端生产入口切换和 `api-gateway` 退役准备，readiness 必须额外暴露生产入口切换预检摘要。仓库内没有可提交的前端、反向代理或部署入口配置时，`productionEntrypointCutoverPrecheckStatus` 固定为 `BLOCKED_BY_MISSING_EXTERNAL_ENTRYPOINT_CONFIG`。`productionEntrypointCutoverPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_READY`、`BUSINESS_PATHS_PRESERVED`、`REAL_HTTP_REHEARSAL_PASSED` 和 `API_GATEWAY_ROLLBACK_TARGET_DEFINED` 必须为 `PASS`；`EXTERNAL_ENTRYPOINT_CONFIG_PRESENT`、`TRAFFIC_SWITCH_APPLIED` 和 `API_GATEWAY_RETIREMENT_APPROVED` 必须为 `BLOCKED`。`productionEntrypointCutoverEvidence` 必须包含 `targetEntrypoint=unified-backend:8135`、`currentEntrypoint=api-gateway:8125`、`businessPathsRemainUnchanged=true`、`externalEntrypointConfigPresent=false`、`trafficSwitchApplied=false`、`rollbackTarget=api-gateway:8125`、`apiGatewayRetirementApproved=false`、`readyForProduction=false`、`readyToReplaceGateway=false` 和 `status=BLOCKED_BY_MISSING_EXTERNAL_ENTRYPOINT_CONFIG`。该 evidence 不得读取真实环境变量，不得探测真实外部代理，不得伪造前端或代理切换结果，不得返回 token、Cookie、内部签名、本地用户目录、节点密钥、真实终端命令或真实文件路径。

同一轮必须额外暴露 `api-gateway` 退役预检摘要。`apiGatewayRetirementPrecheckStatus` 在缺少真实切流证据时固定为 `BLOCKED_BY_TRAFFIC_NOT_SWITCHED`。`apiGatewayRetirementPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `API_GATEWAY_ROLLBACK_ROLE_PROTECTED` 和 `API_GATEWAY_SELF_APIS_MOUNTED_IN_UNIFIED` 必须为 `PASS`；`PRODUCTION_ENTRYPOINT_SWITCH_APPLIED`、`ROLLBACK_WINDOW_COMPLETED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`USER_RETIREMENT_APPROVAL_GRANTED` 和 `DELETE_LIST_CONFIRMED` 必须为 `BLOCKED`。`apiGatewayRetirementEvidence` 必须包含 `retirementApprovalStatus=BLOCKED`、`trafficSwitchApplied=false`、`trafficSwitchProven=false`、`rollbackWindowCompleted=false`、`apiGatewayRetirementApproved=false`、`protectedEntrypoint=api-gateway:8125`、`nextAction=WAIT_FOR_UNIFIED_ENTRYPOINT_TRAFFIC_SWITCH`、`deletionAllowed=false` 和 `status=BLOCKED_BY_TRAFFIC_NOT_SWITCHED`。该 evidence 只能作为只读退役门禁，不得把 `api-gateway-service` 或五个 core 描述为可删除。

为第二十六轮统一后端内部合并收口与 core 退役前置证据做准备，readiness 必须额外暴露五个 core 的退役前置矩阵。`coreEntrypointRetirementPrecheckStatus` 在本阶段固定为 `BLOCKED_BY_PROTECTED_ROLLBACK_ROLE`，只表示五个 core 已经在统一后端候选入口内具备可验证覆盖证据，不表示任何 core 可以删除。`coreEntrypointRetirementPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_IN_PROCESS_COVERAGE`、`CORE_SELF_APIS_MOUNTED`、`BUSINESS_PATHS_PRESERVED`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED` 和 `INDEPENDENT_CORE_REGRESSION_REQUIRED` 必须为 `PASS`；`API_GATEWAY_RETIREMENT_COMPLETED`、`EXTERNAL_ENTRYPOINT_TRAFFIC_SWITCHED`、`ROLLBACK_WINDOW_COMPLETED`、`USER_CORE_RETIREMENT_APPROVAL_GRANTED` 和 `CORE_DELETE_LIST_CONFIRMED` 必须为 `BLOCKED`。`coreEntrypointRetirementEvidence` 必须包含 `retirementApprovalStatus=BLOCKED`、`deletionAllowed=false`、`trafficSwitchApplied=false`、`apiGatewayRetired=false`、`rollbackWindowCompleted=false`、`bulkRetirementAllowed=false`、`nextEligibleCore=NONE_UNTIL_API_GATEWAY_RETIRED`、`protectedCoreEntrypoints`、`coreEntrypointMatrix` 和 `status=BLOCKED_BY_PROTECTED_ROLLBACK_ROLE`。`coreEntrypointMatrix` 必须列出 `business-core:8130`、`admission-core:8131`、`engagement-core:8132`、`ops-core:8133` 和 `portal-core:8134`，每项包含 `entrypointKey`、`port`、`sourceDirectory`、`hostedRouteIds`、`inProcessMountedInUnified=true`、`selfApisMountedInUnified=true`、`independentRegressionRequired=true`、`retirementOrder`、`retirementStatus=BLOCKED` 和 `blockedBy=PROTECTED_ROLLBACK_ROLE`。该 evidence 不得读取真实外部配置，不得探测真实流量，不得修改业务路径，不得删除旧 Maven 入口，不得把五个 core 描述为可批量删除。

为第二十七轮统一后端生产化硬化与真实切流前置准备，readiness 必须额外暴露生产化硬化总门禁。`productionHardeningPrecheckStatus` 在缺少真实外部入口、集中配置、持久化审计、回滚窗口和退役审批证据时固定为 `BLOCKED_BY_EXTERNAL_PRODUCTION_PREREQUISITES`。`productionHardeningPrecheckChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `UNIFIED_BACKEND_CANDIDATE_READY`、`BUSINESS_PATHS_PRESERVED`、`REAL_HTTP_REHEARSAL_PASSED`、`ROUTE_DRIFT_SCAN_PASSED`、`ROLLBACK_ENTRYPOINTS_PROTECTED`、`CENTRAL_CONFIG_CONTRACT_DEFINED`、`AUDIT_TRAIL_CONTRACT_DEFINED`、`CUTOVER_RUNBOOK_DEFINED`、`PRODUCTION_CUTOVER_APPROVAL_PACKAGE_RECORDED`、`CUTOVER_EVIDENCE_CONSISTENCY_AUDIT_RECORDED`、`EXTERNAL_VALUE_INTAKE_REHEARSAL_RECORDED`、`PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_RECORDED`、`ROLLBACK_RECHECK_COMMANDS_DEFINED` 和 `SMOKE_EVIDENCE_FORMAT_DEFINED` 必须为 `PASS`；`CENTRAL_CONFIG_PROVIDER_CONNECTED`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`PERSISTENT_AUDIT_SINK_CONNECTED`、`AUDIT_WRITE_PATH_CONNECTED`、`EXTERNAL_ENTRYPOINT_CONFIG_PRESENT`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`ROLLBACK_WINDOW_COMPLETED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN` 和 `USER_RETIREMENT_APPROVAL_GRANTED` 必须为 `BLOCKED`。`productionHardeningEvidence` 必须包含 `candidateEntrypoint=unified-backend:8135`、`currentEntrypoint=api-gateway:8125`、`rollbackEntrypoints=["api-gateway:8125","business-core:8130","admission-core:8131","engagement-core:8132","ops-core:8133","portal-core:8134"]`、`businessPathsRemainUnchanged=true`、`centralConfigProviderConnected=false`、`sensitiveConfigExternalized=false`、`persistentAuditSinkConnected=false`、`auditWritePathConnected=false`、`externalEntrypointConfigPresent=false`、`trafficSwitchApplied=false`、`rollbackWindowCompleted=false`、`apiGatewayTrafficZeroProven=false`、`apiGatewayRetirementApproved=false`、`coreRetirementApproved=false`、`smokeEvidenceRecorded=true`、`runbookRecorded=true`、`deletionAllowed=false` 和 `status=BLOCKED_BY_EXTERNAL_PRODUCTION_PREREQUISITES`。该 evidence 借鉴渐进切流、网关回滚和配置外置的成熟做法，但只记录仓库内可验证的脱敏证据，不得读取真实环境变量，不得探测真实外部代理，不得写真实审计，不得伪造生产切流、回滚窗口、流量归零或退役审批结果，不得返回 token、Cookie、内部签名、本地用户目录、节点密钥、数据库连接串、真实终端命令或真实文件路径。

为第三十六轮统一后端生产切流审批包和外部参数清单做准备，readiness 必须额外暴露生产切流审批包门禁。`productionCutoverApprovalPackageStatus` 在仓库内样板、外部参数清单、审批角色、go/no-go 矩阵、观测字段、回滚授权和退役门禁都可解析且脱敏时固定为 `PASS_LOCAL_APPROVAL_PACKAGE_REHEARSAL_NOT_PRODUCTION`。该状态只表示审批包 evidence 可本地验证，不表示真实审批已经完成，不表示真实前端、反向代理或部署入口已经切到 `unified-backend:8135`，不表示真实集中配置 provider、生产 profile、真实持久化 audit sink 或生产流量已经接入。

`productionCutoverApprovalPackageChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `APPROVAL_PACKAGE_SAMPLE_PRESENT`、`APPROVAL_PACKAGE_JSON_PARSABLE`、`EXISTING_LOCAL_EVIDENCE_REFERENCED`、`EXTERNAL_PARAMETER_CHECKLIST_RECORDED`、`APPROVAL_ROLES_RECORDED`、`GO_NO_GO_MATRIX_RECORDED`、`OBSERVATION_CHECKLIST_RECORDED`、`ROLLBACK_AUTHORITY_RECORDED`、`RETIREMENT_GATE_RECORDED`、`NO_SENSITIVE_VALUES_IN_APPROVAL_PACKAGE` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`；`REAL_EXTERNAL_ENTRYPOINT_VALUES_NOT_PROVIDED`、`REAL_CENTRAL_CONFIG_PROVIDER_NOT_CONNECTED`、`REAL_AUDIT_SINK_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_APPROVED`、`ROLLBACK_OPERATOR_NOT_APPROVED` 和 `RETIREMENT_APPROVER_NOT_GRANTED` 必须为 `BLOCKED`。

`productionCutoverApprovalPackageEvidence` 必须包含 `readinessMode=LOCAL_PRODUCTION_CUTOVER_APPROVAL_PACKAGE_REHEARSAL_NOT_PRODUCTION`、`sampleApprovalPackagePath=docs/unified-backend-production-cutover-approval-package-sample.json`、`sampleApprovalPackagePresent=true`、`sampleApprovalPackageParsed=true`、`approvalPackageApplied=false`、`productionTrafficAllowed=false`、`requiresUserApprovalBeforeApply=true`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`existingEvidenceReferencedTotal`、`externalParametersTotal`、`approvalRolesTotal`、`goNoGoItemsTotal`、`observationFieldsTotal`、`verificationCommandsTotal=7`、`externalEntrypointValuesProvided=false`、`centralConfigProviderConnected=false`、`productionProfileBound=false`、`sensitiveConfigExternalized=false`、`persistentAuditSinkConnected=false`、`auditWriteSmokePassed=false`、`productionTrafficApproved=false`、`rollbackOperatorApproved=false`、`retirementApproverGranted=false`、`deletionAllowed=false`、`bulkRetirementAllowed=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_APPROVAL_PACKAGE_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPROVED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_OPERATOR_APPROVED`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。该 evidence 不得读取真实环境变量，不得连接真实外部配置，不得写真实审计，不得执行真实切流、回滚、代理刷新、部署平台命令或入口退役，不得返回 token、Cookie、内部签名、本地用户目录、节点密钥、数据库连接串、真实终端命令或真实文件路径。

审批包正式样板文件为 `docs/unified-backend-production-cutover-approval-package-sample.json`。样板必须是普通 JSON，`mode` 固定为 `LOCAL_APPROVAL_PACKAGE_REHEARSAL_NOT_APPLIED`，`approvalPackageApplied=false`，`productionTrafficAllowed=false`，`requiresUserApprovalBeforeApply=true`。样板必须记录本地 evidence 引用、外部参数清单、审批角色、go/no-go 矩阵、观测字段、回滚授权、旧入口退役门禁、7 个 Maven 验证命令和安全策略。样板只能写非生产占位口径，不得提交真实域名、真实 IP、真实上游、真实配置值、真实审批人、真实观测面板、真实运行命令、真实凭据或本地用户目录。

为第三十七轮统一后端外部参数接收器与脱敏校验做准备，readiness 必须额外暴露生产切流外部参数 manifest 门禁。`productionCutoverExternalParameterManifestStatus` 在仓库内非生产 manifest 样板可解析、参数槽位完整、真实值未入仓、审批包有关联且脱敏校验通过时固定为 `PASS_REDACTED_EXTERNAL_PARAMETER_MANIFEST_REHEARSAL_NOT_PRODUCTION`。该状态只表示外部参数结构、引用名、阻塞项和脱敏规则可本地验证，不表示真实前端 base URL、真实反向代理 upstream、真实部署入口、真实集中配置 provider、生产 profile、真实 audit sink、生产观测面板或真实审批已经提供。

`productionCutoverExternalParameterManifestChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `EXTERNAL_PARAMETER_MANIFEST_SAMPLE_PRESENT`、`EXTERNAL_PARAMETER_MANIFEST_JSON_PARSABLE`、`PARAMETER_GROUPS_RECORDED`、`FRONTEND_ENTRYPOINT_PARAMETER_RECORDED`、`PROXY_UPSTREAM_PARAMETER_RECORDED`、`DEPLOYMENT_ENTRYPOINT_PARAMETER_RECORDED`、`CENTRAL_CONFIG_PROVIDER_PARAMETER_RECORDED`、`PRODUCTION_PROFILE_PARAMETER_RECORDED`、`SENSITIVE_CONFIG_EXTERNALIZATION_PARAMETER_RECORDED`、`AUDIT_SINK_PARAMETER_RECORDED`、`OBSERVABILITY_PARAMETER_RECORDED`、`APPROVAL_REFERENCE_RECORDED`、`ROLLBACK_AUTHORITY_REFERENCE_RECORDED`、`RETIREMENT_APPROVAL_REFERENCE_RECORDED`、`NO_REAL_VALUES_IN_REPOSITORY` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`；`REAL_EXTERNAL_ENTRYPOINT_VALUES_NOT_PROVIDED`、`REAL_CENTRAL_CONFIG_PROVIDER_NOT_CONNECTED`、`REAL_AUDIT_SINK_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN` 和 `RETIREMENT_NOT_APPROVED` 必须为 `BLOCKED`。

`productionCutoverExternalParameterManifestEvidence` 必须包含 `readinessMode=LOCAL_EXTERNAL_PARAMETER_MANIFEST_REHEARSAL_NOT_PRODUCTION`、`sampleManifestPath=docs/unified-backend-production-cutover-external-parameters-sample.json`、`sampleManifestPresent=true`、`sampleManifestParsed=true`、`manifestApplied=false`、`productionTrafficAllowed=false`、`realValuesAllowedInRepository=false`、`requiresExternalSecretStore=true`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`parameterGroupsTotal`、`parametersTotal`、`requiredExternalParametersTotal`、`realValuesProvidedInRepository=false`、`redactedParametersTotal`、`approvalPackageReferenced=true`、`approvalPackageApplied=false`、`productionTrafficApproved=false`、`centralConfigProviderConnected=false`、`productionProfileBound=false`、`sensitiveConfigExternalized=false`、`persistentAuditSinkConnected=false`、`auditWriteSmokePassed=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`retirementApproverGranted=false`、`deletionAllowed=false`、`bulkRetirementAllowed=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_REDACTED_EXTERNAL_PARAMETER_MANIFEST_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_EXTERNAL_ENTRYPOINT_CONFIG_VALUES_PROVIDED_OUTSIDE_REPOSITORY`、`REAL_EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPROVED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_OPERATOR_APPROVED`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。

外部参数 manifest 正式样板文件为 `docs/unified-backend-production-cutover-external-parameters-sample.json`。样板必须是普通 JSON，`mode` 固定为 `LOCAL_EXTERNAL_PARAMETER_MANIFEST_REHEARSAL_NOT_APPLIED`，`manifestApplied=false`，`productionTrafficAllowed=false`，`realValuesAllowedInRepository=false`，`requiresExternalSecretStore=true`。样板只允许记录参数组、参数 key、外部引用名、是否阻塞切流、审批包引用、go/no-go 影响、验证命令和脱敏策略，不得提交真实域名、真实 IP、真实 upstream、真实 dashboard URL、真实部署平台地址、真实数据库连接串、真实 audit sink 地址、真实 token、真实 Cookie、真实内部签名、真实节点密钥、真实用户目录、真实终端命令或本地绝对路径。外部参数 manifest adapter 不得读取真实环境变量，不得读取 `.env`，不得发起真实 HTTP 探测，不得写真实审计，不得执行真实切流、回滚、代理刷新、部署平台命令或入口退役。

为第三十八轮统一后端真实切流前证据一致性审计做准备，readiness 必须额外暴露 `productionCutoverEvidenceConsistencyAuditStatus`、`productionCutoverEvidenceConsistencyAuditChecks` 和 `productionCutoverEvidenceConsistencyAuditEvidence`。`productionCutoverEvidenceConsistencyAuditStatus` 在仓库内脱敏样板可加载、可解析，且外部入口样板、集中配置样板、audit sink 样板、生产切流 runbook、审批包和外部参数 manifest 的入口、验证命令、参数引用、阻塞项与脱敏口径一致时固定为 `PASS_LOCAL_CUTOVER_EVIDENCE_CONSISTENCY_AUDIT_NOT_PRODUCTION`。该状态只表示本地证据之间没有漂移，不表示真实外部参数、真实集中配置 provider、真实 audit sink、生产流量、流量归零、回滚窗口或退役审批已经完成。

`productionCutoverEvidenceConsistencyAuditChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CUTOVER_EVIDENCE_SAMPLES_PRESENT`、`CUTOVER_EVIDENCE_SAMPLES_PARSEABLE`、`CANDIDATE_ENTRYPOINT_CONSISTENT`、`CURRENT_ENTRYPOINT_CONSISTENT`、`ROLLBACK_ENTRYPOINT_CONSISTENT`、`MAVEN_REGRESSION_COMMANDS_CONSISTENT`、`EXTERNAL_PARAMETER_KEYS_REFERENCED_BY_APPROVAL_PACKAGE`、`RUNBOOK_REFERENCES_EXTERNAL_PARAMETER_MANIFEST`、`CENTRAL_CONFIG_KEYS_COVERED_BY_MANIFEST`、`AUDIT_SINK_KEYS_COVERED_BY_MANIFEST`、`OBSERVABILITY_KEYS_COVERED_BY_RUNBOOK_AND_MANIFEST`、`RETIREMENT_AND_ROLLBACK_GATES_CONSISTENT`、`NO_REAL_VALUES_IN_CUTOVER_EVIDENCE` 和 `READY_FLAGS_REMAIN_FALSE` 必须为 `PASS`；`REAL_EXTERNAL_VALUES_NOT_IMPORTED`、`REAL_CENTRAL_CONFIG_PROVIDER_NOT_CONNECTED`、`REAL_AUDIT_SINK_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN` 和 `RETIREMENT_NOT_APPROVED` 必须为 `BLOCKED`。

`productionCutoverEvidenceConsistencyAuditEvidence` 必须包含 `readinessMode=LOCAL_CUTOVER_EVIDENCE_CONSISTENCY_AUDIT_NOT_PRODUCTION`、`auditedSamplePaths`、`samplesPresent=true`、`samplesParsed=true`、`candidateEntrypoint=http://127.0.0.1:8135`、`currentEntrypoint=http://127.0.0.1:8125`、`rollbackEntrypoint=http://127.0.0.1:8125`、`externalParameterKeysTotal`、`approvalPackageExternalParametersTotal`、`runbookVerificationCommandsTotal=7`、`manifestVerificationCommandsTotal=7`、`missingApprovalParameterKeys`、`missingManifestParameterKeys`、`inconsistentEntrypointRefs`、`inconsistentVerificationCommands`、`inconsistentBlockers`、`realValuesProvidedInRepository=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_LOCAL_CUTOVER_EVIDENCE_CONSISTENCY_AUDIT_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_EXTERNAL_ENTRYPOINT_CONFIG_VALUES_PROVIDED_OUTSIDE_REPOSITORY`、`REAL_EXTERNAL_ENTRYPOINT_CONFIG_APPLIED`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPROVED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_OPERATOR_APPROVED`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。本地审计 adapter 只能读取仓库内正式样板，不得读取真实环境变量，不得读取 `.env`，不得发起真实 HTTP 探测，不得写真实审计，不得执行真实切流、回滚、代理刷新、部署平台命令或入口退役。

为第三十九轮统一后端生产外部值接收演练做准备，readiness 必须额外暴露 `productionExternalValueIntakeRehearsalStatus`、`productionExternalValueIntakeRehearsalChecks` 和 `productionExternalValueIntakeRehearsalEvidence`。`productionExternalValueIntakeRehearsalStatus` 在仓库内外部值接收样板可解析、槽位完整、引用完整、注入目标完整、验证引用完整、回滚引用完整且脱敏校验通过时固定为 `PASS_EXTERNAL_VALUE_INTAKE_REHEARSAL_NOT_PRODUCTION`。该状态只表示外部值接收流程和安全交接槽位可本地验证，不表示真实外部值已提供，不表示真实生产入口已应用，不表示真实集中配置 provider、真实 audit sink、生产流量、网关流量归零、回滚窗口或退役审批已经完成。

`productionExternalValueIntakeRehearsalChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `EXTERNAL_VALUE_INTAKE_SAMPLE_PRESENT`、`EXTERNAL_VALUE_INTAKE_SAMPLE_JSON_PARSABLE`、`VALUE_GROUPS_RECORDED`、`INTAKE_CHANNELS_RECORDED`、`INJECTION_TARGETS_RECORDED`、`VALIDATION_REFS_RECORDED`、`ROLLBACK_REFS_RECORDED`、`NO_REAL_VALUES_IN_REPOSITORY`、`NO_SENSITIVE_VALUES_IN_INTAKE_SAMPLE`、`READY_FLAGS_REMAIN_FALSE` 和 `EXTERNAL_VALUE_INTAKE_REHEARSAL_RECORDED` 必须为 `PASS`；`REAL_EXTERNAL_VALUES_NOT_PROVIDED`、`REAL_ENTRYPOINT_NOT_APPLIED`、`REAL_CENTRAL_CONFIG_PROVIDER_NOT_CONNECTED`、`REAL_AUDIT_SINK_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`ROLLBACK_WINDOW_NOT_COMPLETED` 和 `RETIREMENT_NOT_APPROVED` 必须为 `BLOCKED`。

`productionExternalValueIntakeRehearsalEvidence` 必须包含 `readinessMode=LOCAL_EXTERNAL_VALUE_INTAKE_REHEARSAL_NOT_PRODUCTION`、`sampleIntakePath=docs/unified-backend-production-external-value-intake-sample.json`、`sampleIntakePresent=true`、`sampleIntakeParsed=true`、`intakeApplied=false`、`productionTrafficAllowed=false`、`realValuesAllowedInRepository=false`、`requiresExternalSecretStore=true`、`candidateEntrypointRef=LOCAL_SAMPLE_REF:UNIFIED_BACKEND_8135`、`currentEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`rollbackEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`valueGroupsTotal`、`intakeChannelsTotal`、`valueItemsTotal`、`injectionTargetsTotal`、`validationRefsTotal`、`rollbackRefsTotal`、`realValuesProvidedInRepository=false`、`redactedValuesTotal`、`centralConfigProviderConnected=false`、`productionProfileBound=false`、`sensitiveConfigExternalized=false`、`persistentAuditSinkConnected=false`、`auditWriteSmokePassed=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`retirementApproverGranted=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_EXTERNAL_VALUE_INTAKE_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_EXTERNAL_VALUES_PROVIDED_OUTSIDE_REPOSITORY`、`REAL_EXTERNAL_VALUES_APPLIED_TO_RUNTIME`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`PRODUCTION_PROFILE_BOUND`、`SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。本地接收演练 adapter 只能读取仓库内正式样板，不得读取真实环境变量，不得读取 `.env`，不得连接真实配置或审计系统，不得发起真实 HTTP 探测，不得执行真实切流、回滚、代理刷新、部署平台命令或入口退役。

为第四十轮统一后端生产运行壳和配置接入演练做准备，readiness 必须额外暴露 `productionRuntimeConfigShellStatus`、`productionRuntimeConfigShellChecks` 和 `productionRuntimeConfigShellEvidence`。`productionRuntimeConfigShellStatus` 在仓库内生产运行壳样板可解析、生产 profile 槽位、集中配置 provider 槽位、敏感配置外置槽位、部署入口槽位、回滚配置槽位、第三十九轮外部值接收演练引用和脱敏校验都通过时固定为 `PASS_PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_NOT_PRODUCTION`。该状态只表示生产运行壳和配置接入形状可本地验证，不表示真实生产 profile 已绑定，不表示真实集中配置 provider 已连接，不表示真实密钥已外置，不表示部署入口或回滚配置已应用，也不表示可以替代 `api-gateway`。

`productionRuntimeConfigShellChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `PRODUCTION_RUNTIME_SHELL_SAMPLE_PRESENT`、`PRODUCTION_RUNTIME_SHELL_SAMPLE_JSON_PARSABLE`、`RUNTIME_PROFILE_SLOT_RECORDED`、`CENTRAL_CONFIG_PROVIDER_SLOT_RECORDED`、`SENSITIVE_CONFIG_EXTERNALIZATION_SLOT_RECORDED`、`DEPLOYMENT_ENTRYPOINT_SLOT_RECORDED`、`ROLLBACK_CONFIG_SLOT_RECORDED`、`EXTERNAL_VALUE_INTAKE_REHEARSAL_REFERENCED`、`NO_REAL_VALUES_IN_REPOSITORY`、`NO_SENSITIVE_VALUES_IN_RUNTIME_SHELL_SAMPLE`、`READY_FLAGS_REMAIN_FALSE` 和 `PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_RECORDED` 必须为 `PASS`；`REAL_PRODUCTION_PROFILE_NOT_BOUND`、`REAL_CENTRAL_CONFIG_PROVIDER_NOT_CONNECTED`、`REAL_SENSITIVE_CONFIG_NOT_EXTERNALIZED`、`REAL_DEPLOYMENT_ENTRYPOINT_NOT_BOUND`、`REAL_ROLLBACK_CONFIG_NOT_BOUND`、`REAL_AUDIT_SINK_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`ROLLBACK_WINDOW_NOT_COMPLETED` 和 `RETIREMENT_NOT_APPROVED` 必须为 `BLOCKED`。

`productionRuntimeConfigShellEvidence` 必须包含 `readinessMode=LOCAL_PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_NOT_PRODUCTION`、`sampleRuntimeShellPath=docs/unified-backend-production-runtime-shell-sample.json`、`sampleRuntimeShellPresent=true`、`sampleRuntimeShellParsed=true`、`runtimeShellApplied=false`、`productionTrafficAllowed=false`、`realValuesAllowedInRepository=false`、`requiresExternalConfigProvider=true`、`requiresExternalSecretStore=true`、`candidateEntrypointRef=LOCAL_SAMPLE_REF:UNIFIED_BACKEND_8135`、`currentEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`rollbackEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`runtimeProfilesTotal`、`configProviderBindingsTotal`、`sensitiveConfigBindingsTotal`、`deploymentEntrypointBindingsTotal`、`rollbackConfigBindingsTotal`、`validationCommandsTotal`、`productionProfileBound=false`、`centralConfigProviderConnected=false`、`sensitiveConfigExternalized=false`、`deploymentEntrypointBound=false`、`rollbackConfigBound=false`、`persistentAuditSinkConnected=false`、`auditWriteSmokePassed=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`retirementApproverGranted=false`、`environmentVariablesRead=false`、`realValuesProvidedInRepository=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_PRODUCTION_PROFILE_BOUND_OUTSIDE_REPOSITORY`、`REAL_CENTRAL_CONFIG_PROVIDER_CONNECTED`、`REAL_SENSITIVE_CONFIG_SOURCE_EXTERNALIZED`、`REAL_DEPLOYMENT_ENTRYPOINT_BOUND`、`REAL_ROLLBACK_CONFIG_BOUND`、`REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。本地运行壳 adapter 只能读取仓库内正式样板和第三十九轮样板，不得读取真实环境变量，不得读取 `.env`，不得连接真实配置、密钥、部署或审计系统，不得发起真实 HTTP 探测，不得执行真实部署、代理刷新、切流、回滚或入口退役。

为第四十一轮统一后端生产审计和观测 smoke 闭环做准备，readiness 必须额外暴露 `productionAuditObservabilitySmokeStatus`、`productionAuditObservabilitySmokeChecks` 和 `productionAuditObservabilitySmokeEvidence`。`productionAuditObservabilitySmokeStatus` 在仓库内生产审计和观测 smoke 样板可解析、第四十轮运行壳演练被引用、审计 sink 绑定、审计事件 schema、request id 贯穿、审计写入 smoke 引用、回放导出保留引用、HTTP smoke、错误率、延迟、业务码、trace、dashboard、alert、rollback 和脱敏校验都通过时固定为 `PASS_PRODUCTION_AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_NOT_PRODUCTION`。该状态只表示生产审计和观测 smoke 形状可本地验证，不表示真实持久化 audit sink 已连接，不表示真实审计事件已写入生产存储，不表示真实观测平台、dashboard、alert 或 trace 管道可用，也不表示生产流量已经切到 `unified-backend:8135`。

`productionAuditObservabilitySmokeChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `AUDIT_OBSERVABILITY_SMOKE_SAMPLE_PRESENT`、`AUDIT_OBSERVABILITY_SMOKE_SAMPLE_JSON_PARSABLE`、`RUNTIME_CONFIG_SHELL_REHEARSAL_REFERENCED`、`AUDIT_SINK_BINDING_RECORDED`、`AUDIT_EVENT_SCHEMA_RECORDED`、`REQUEST_ID_PROPAGATION_RECORDED`、`AUDIT_WRITE_SMOKE_REFERENCE_RECORDED`、`AUDIT_REPLAY_EXPORT_RETENTION_RECORDED`、`HTTP_SMOKE_OBSERVATION_RECORDED`、`ERROR_RATE_OBSERVATION_RECORDED`、`LATENCY_OBSERVATION_RECORDED`、`BUSINESS_CODE_OBSERVATION_RECORDED`、`TRACE_CORRELATION_RECORDED`、`DASHBOARD_AND_ALERT_REFERENCES_RECORDED`、`NO_SENSITIVE_VALUES_IN_AUDIT_OBSERVABILITY_SAMPLE`、`READY_FLAGS_REMAIN_FALSE` 和 `PRODUCTION_AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_RECORDED` 必须为 `PASS`；`REAL_PERSISTENT_AUDIT_SINK_NOT_CONNECTED`、`REAL_AUDIT_WRITE_PATH_NOT_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_NOT_PASSED`、`REAL_AUDIT_REPLAY_EXPORT_RETENTION_NOT_CONNECTED`、`REAL_OBSERVABILITY_PLATFORM_NOT_CONNECTED`、`REAL_DASHBOARD_NOT_CONNECTED`、`REAL_ALERTING_NOT_CONNECTED`、`REAL_TRACE_PIPELINE_NOT_CONNECTED`、`PRODUCTION_TRAFFIC_NOT_SWITCHED`、`PRODUCTION_TRAFFIC_NOT_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`ROLLBACK_WINDOW_NOT_COMPLETED` 和 `RETIREMENT_NOT_APPROVED` 必须为 `BLOCKED`。

`productionAuditObservabilitySmokeEvidence` 必须包含 `readinessMode=LOCAL_PRODUCTION_AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_NOT_PRODUCTION`、`sampleAuditObservabilitySmokePath=docs/unified-backend-production-audit-observability-smoke-sample.json`、`sampleAuditObservabilitySmokePresent=true`、`sampleAuditObservabilitySmokeParsed=true`、`runtimeConfigShellSampleRef=docs/unified-backend-production-runtime-shell-sample.json`、`runtimeConfigShellStatusRequired=PASS_PRODUCTION_RUNTIME_CONFIG_SHELL_REHEARSAL_NOT_PRODUCTION`、`auditSinkBindingRecorded=true`、`auditEventSchemaRecorded=true`、`requestIdPropagationRecorded=true`、`auditWriteSmokeReferenceRecorded=true`、`auditReplayExportRetentionRecorded=true`、`httpSmokeObservationRecorded=true`、`errorRateObservationRecorded=true`、`latencyObservationRecorded=true`、`businessCodeObservationRecorded=true`、`traceCorrelationRecorded=true`、`dashboardReferencesRecorded=true`、`alertReferencesRecorded=true`、`sampleAuditSmokeTargetsTotal`、`sampleObservabilitySignalsTotal`、`sampleDashboardRefsTotal`、`sampleAlertRefsTotal`、`sampleRollbackRefsTotal`、`persistentAuditSinkConnected=false`、`auditWritePathConnected=false`、`auditWriteSmokePassed=false`、`auditReplayPathConnected=false`、`auditExportPathConnected=false`、`auditRetentionJobConnected=false`、`observabilityPlatformConnected=false`、`dashboardConnected=false`、`alertingConnected=false`、`tracePipelineConnected=false`、`environmentVariablesRead=false`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`retirementApproverGranted=false`、`realValuesProvidedInRepository=false`、`sensitiveValuesExposed=false`、`readyForProduction=false`、`readyToReplaceGateway=false`、`remainingProductionBlockers` 和 `status=PASS_PRODUCTION_AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_NOT_PRODUCTION`。`remainingProductionBlockers` 至少包含 `REAL_PERSISTENT_AUDIT_SINK_CONNECTED`、`REAL_AUDIT_WRITE_PATH_CONNECTED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`REAL_AUDIT_REPLAY_EXPORT_RETENTION_CONNECTED`、`REAL_OBSERVABILITY_PLATFORM_CONNECTED`、`REAL_DASHBOARD_CONNECTED`、`REAL_ALERTING_CONNECTED`、`REAL_TRACE_PIPELINE_CONNECTED`、`PRODUCTION_TRAFFIC_SWITCH_APPLIED`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN`、`ROLLBACK_WINDOW_COMPLETED` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。本地审计观测 smoke adapter 只能读取仓库内正式样板和第四十轮运行壳样板，不得读取真实环境变量，不得读取 `.env`，不得连接真实 audit sink、日志平台、指标平台、trace 平台、dashboard、告警或部署平台，不得发起真实外部 HTTP 探测，不得写真实审计，不得执行真实切流、回滚或入口退役。

生产审计和观测 smoke 正式样板文件为 `docs/unified-backend-production-audit-observability-smoke-sample.json`。样板必须是普通 JSON，`mode` 固定为 `LOCAL_PRODUCTION_AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_NOT_CONNECTED`，`productionTrafficAllowed=false`，`realValuesAllowedInRepository=false`。样板只允许记录脱敏引用、审计事件 schema、审计 smoke 目标、观测信号、dashboard 引用、alert 引用、rollback 引用、失败降级和脱敏规则，不得提交真实 URL、真实 IP、真实域名、真实 token、真实 Cookie、真实数据库连接串、真实 sink DSN、真实 dashboard 地址、真实日志索引、真实 topic、真实 bucket、真实用户目录或真实终端命令。

为第四十二轮统一后端受控生产入口切流与回滚窗口闭环做准备，readiness 必须额外暴露 `productionControlledCutoverStatus`、`productionControlledCutoverChecks` 和 `productionControlledCutoverEvidence`。`productionControlledCutoverStatus` 在只有仓库内脱敏收据样板、没有真实外部切流收据时固定为 `BLOCKED_BY_REAL_CUTOVER_RECEIPT_NOT_PROVIDED`。该状态只表示候选入口已具备受控生产切流收据接收和校验能力，不表示真实生产入口已经指向 `unified-backend:8135`，不表示生产流量已经经过候选入口，不表示真实 audit sink、dashboard、alert 或 trace 管道已经完成生产验证，也不表示 `api-gateway` 或五个 core 可以退役。

`productionControlledCutoverChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `CONTROLLED_CUTOVER_RECEIPT_SAMPLE_PRESENT`、`CONTROLLED_CUTOVER_RECEIPT_SAMPLE_JSON_PARSABLE`、`RUNTIME_CONFIG_SHELL_REHEARSAL_REFERENCED`、`AUDIT_OBSERVABILITY_SMOKE_REHEARSAL_REFERENCED`、`EXTERNAL_VALUE_INTAKE_REHEARSAL_REFERENCED`、`APPROVAL_REFS_RECORDED`、`TRAFFIC_PLAN_RECORDED`、`SMOKE_REFS_RECORDED`、`AUDIT_REFS_RECORDED`、`OBSERVABILITY_REFS_RECORDED`、`ROLLBACK_WINDOW_REFS_RECORDED`、`OLD_ENTRYPOINT_PROTECTION_RECORDED`、`NO_REAL_VALUES_IN_REPOSITORY`、`NO_SENSITIVE_VALUES_IN_CONTROLLED_CUTOVER_RECEIPT`、`READY_FLAGS_REMAIN_FALSE` 和 `CONTROLLED_CUTOVER_RECEIPT_GATE_RECORDED` 必须为 `PASS`；`REAL_CUTOVER_RECEIPT_NOT_PROVIDED`、`REAL_ENTRYPOINT_NOT_APPLIED`、`PRODUCTION_TRAFFIC_NOT_OBSERVED_ON_UNIFIED`、`REAL_AUDIT_WRITE_SMOKE_NOT_PASSED`、`REAL_DASHBOARD_NOT_VERIFIED`、`REAL_ALERTING_NOT_VERIFIED`、`REAL_TRACE_PIPELINE_NOT_VERIFIED`、`ROLLBACK_WINDOW_NOT_COMPLETED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`RETIREMENT_NOT_APPROVED` 和 `OLD_ENTRYPOINTS_NOT_IN_RETIREMENT_ROUND` 必须为 `BLOCKED`。

`productionControlledCutoverEvidence` 必须包含 `readinessMode=LOCAL_CONTROLLED_CUTOVER_RECEIPT_GATE_NOT_PRODUCTION`、`receiptPath=docs/unified-backend-production-controlled-cutover-receipt-sample.json`、`receiptPresent=true`、`receiptParsed=true`、`receiptApplied=false`、`productionTrafficAllowed=false`、`realValuesAllowedInRepository=false`、`candidateEntrypointRef=LOCAL_SAMPLE_REF:UNIFIED_BACKEND_8135`、`previousEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`rollbackEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`approvalRefsTotal`、`runtimePrerequisiteRefsTotal`、`trafficStagesTotal`、`smokeRefsTotal`、`auditRefsTotal`、`observabilityRefsTotal`、`rollbackWindowRefsTotal`、`apiGatewayTrafficRefsTotal`、`cutoverExecutionRefsTotal`、`finalTrafficWeightPercent=0`、`productionTrafficObservedOnUnified=false`、`apiGatewayTrafficZeroProven=false`、`rollbackWindowCompleted=false`、`persistentAuditSinkConnected=false`、`auditWriteSmokePassed=false`、`observabilityPlatformConnected=false`、`dashboardConnected=false`、`alertingConnected=false`、`tracePipelineConnected=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`oldEntrypointsPreserved=true`、`nodeDaemonOutOfRepository=true`、`readyForProduction=false`、`readyToReplaceGateway=false`、`readyToRetireOldEntrypoints=false`、`remainingBlockers` 和 `status=BLOCKED_BY_REAL_CUTOVER_RECEIPT_NOT_PROVIDED`。`remainingBlockers` 至少包含 `REAL_CUTOVER_RECEIPT_PROVIDED_OUTSIDE_REPOSITORY`、`REAL_ENTRYPOINT_APPLIED_TO_UNIFIED_BACKEND`、`PRODUCTION_TRAFFIC_OBSERVED_ON_UNIFIED`、`REAL_AUDIT_WRITE_SMOKE_PASSED`、`REAL_DASHBOARD_VERIFIED`、`REAL_ALERTING_VERIFIED`、`REAL_TRACE_PIPELINE_VERIFIED`、`ROLLBACK_WINDOW_COMPLETED`、`API_GATEWAY_TRAFFIC_ZERO_PROVEN` 和 `USER_RETIREMENT_APPROVAL_GRANTED`。本地受控切流收据 adapter 只能读取仓库内正式样板或经过脱敏复核的收据结构，不得读取真实环境变量，不得读取 `.env`，不得连接真实配置、审计、日志、指标、trace、dashboard、告警、代理或部署平台，不得发起真实 HTTP 探测，不得执行真实切流、回滚、代理刷新、部署平台命令或入口退役。

为第四十三轮 `api-gateway-service:8125` 受控退役预检做准备，readiness 必须额外暴露 `apiGatewayControlledRetirementStatus`、`apiGatewayControlledRetirementChecks` 和 `apiGatewayControlledRetirementEvidence`。`apiGatewayControlledRetirementStatus` 在只有仓库内脱敏退役收据样板、没有真实外部退役收据和用户删除清单确认时固定为 `BLOCKED_BY_API_GATEWAY_RETIREMENT_RECEIPT_NOT_PROVIDED`。该状态只表示统一后端具备 `api-gateway` 旧入口退役收据、删除清单和残留守卫的本地校验能力，不表示真实生产流量已完成切流，不表示 `api-gateway` 新流量归零，不表示回滚窗口完成，也不允许删除 `backend/api-gateway-service` 或五个 core 入口。

`apiGatewayControlledRetirementChecks` 每项必须包含 `check`、`status`、`detail` 和 `requiredForReplacement`。其中 `API_GATEWAY_RETIREMENT_RECEIPT_SAMPLE_PRESENT`、`API_GATEWAY_RETIREMENT_RECEIPT_SAMPLE_JSON_PARSABLE`、`CONTROLLED_CUTOVER_RECEIPT_REFERENCED`、`GATEWAY_SELF_APIS_PRESERVED_IN_UNIFIED`、`BUSINESS_PATHS_UNCHANGED`、`CORE_ENTRYPOINTS_PRESERVED`、`NODE_DAEMON_OUT_OF_REPOSITORY`、`DELETE_LIST_RECORDED`、`NO_REAL_VALUES_IN_API_GATEWAY_RETIREMENT_RECEIPT`、`NO_SENSITIVE_VALUES_IN_API_GATEWAY_RETIREMENT_RECEIPT`、`READY_FLAGS_REMAIN_FALSE` 和 `UNIFIED_BACKEND_SELF_HOSTS_GATEWAY_SOURCE` 必须为 `PASS`；`RETIREMENT_RECEIPT_NOT_PROVIDED`、`PRODUCTION_TRAFFIC_NOT_OBSERVED_ON_UNIFIED`、`API_GATEWAY_TRAFFIC_ZERO_NOT_PROVEN`、`ROLLBACK_WINDOW_NOT_COMPLETED`、`REAL_AUDIT_WRITE_SMOKE_NOT_PASSED`、`DASHBOARD_ALERT_TRACE_NOT_VERIFIED`、`RETIREMENT_APPROVAL_NOT_GRANTED`、`DELETE_LIST_NOT_APPROVED`、`GATEWAY_SELF_API_PARITY_NOT_PROVEN_WITH_REAL_RECEIPT`、`UNIFIED_BACKEND_FULL_REGRESSION_NOT_RECORDED`、`CORE_ENTRYPOINT_REGRESSION_NOT_RECORDED`、`ROLLBACK_PLAN_NOT_REVALIDATED` 和 `BULK_DELETE_FORBIDDEN` 必须为 `BLOCKED`。

`apiGatewayControlledRetirementEvidence` 必须包含 `readinessMode=LOCAL_API_GATEWAY_RETIREMENT_RECEIPT_GATE_NOT_PRODUCTION`、`receiptPath=docs/unified-backend-api-gateway-retirement-receipt-sample.json`、`receiptPresent=true`、`receiptParsed=true`、`retirementApplied=false`、`deleteListApproved=false`、`productionTrafficAllowed=false`、`realValuesAllowedInRepository=false`、`candidateEntrypointRef=LOCAL_SAMPLE_REF:UNIFIED_BACKEND_8135`、`retiredEntrypointRef=LOCAL_SAMPLE_REF:API_GATEWAY_8125`、`rollbackEntrypointRefs`、`controlledCutoverRefsTotal`、`approvalRefsTotal`、`trafficZeroRefsTotal`、`observabilityRefsTotal`、`auditRefsTotal`、`rollbackWindowRefsTotal`、`gatewaySelfApiParityRefsTotal`、`deleteListItemsTotal`、`deletedFilesTotal=0`、`remainingGatewayPackageRefs=2`、`unifiedBuildHelperStillReferencesApiGateway=false`、`apiGatewayPomStillPresent=true`、`apiGatewayServiceDirectoryStillPresent=true`、`coreEntrypointsPreserved=true`、`readyToRetireBusinessCore=false`、`readyToRetireAdmissionCore=false`、`readyToRetireEngagementCore=false`、`readyToRetireOpsCore=false`、`readyToRetirePortalCore=false`、`bulkDeleteAllowed=false`、`environmentVariablesRead=false`、`sensitiveValuesExposed=false`、`remainingBlockers` 和 `status=BLOCKED_BY_API_GATEWAY_RETIREMENT_RECEIPT_NOT_PROVIDED`。`unified-backend-service` 必须在自身源码树内保留网关控制面源码，不再通过 build-helper 编译 `../api-gateway-service/src/main/java`；但在真实外部退役收据、流量归零、回滚窗口和用户删除清单确认缺失时，`api-gateway-service` 独立入口和目录仍必须保留。本地退役收据 adapter 只能读取仓库内正式样板和第四十二轮受控切流收据样板，不得读取真实环境变量，不得读取 `.env`，不得连接真实代理、部署、审计、日志、指标、trace、dashboard 或告警平台，不得执行真实删除、批量删除、切流、回滚或入口退役。

`apiGatewayRetirementPrecheckStatus` 是第二十五轮留下的长期预检摘要，继续用于说明真实切流前 `api-gateway` 是受保护回滚入口。`apiGatewayControlledRetirementStatus` 是第四十三轮退役收据执行门禁，用于校验退役收据、删除清单、unified 自承载网关源码、core 保护和回归证据。二者都为阻塞时，不得进入删除阶段。

`docs/unified-backend-api-gateway-retirement-receipt-sample.json` 是正式脱敏样板。样板必须固定 `mode=LOCAL_API_GATEWAY_RETIREMENT_RECEIPT_SHAPE_NOT_APPLIED`、`retirementApplied=false`、`deleteListApproved=false`、`productionTrafficAllowed=false` 和 `realValuesAllowedInRepository=false`。样板只能记录脱敏审批引用、第四十二轮受控切流引用、流量归零引用、网关自有 API parity 引用、观测引用、审计引用、回滚窗口引用、删除清单、迁移计划、五个 core 保护和脱敏规则，不得提交真实 URL、真实 IP、真实域名、真实 token、真实 Cookie、真实内部签名、真实连接串、真实 dashboard 地址、真实日志索引、真实 topic、真实 bucket、真实部署命令、真实删除命令、真实 SSH 命令或本地用户目录。删除清单只能列明确文件路径；没有用户确认时不得删除任何文件。

受控生产入口切流收据正式样板文件为 `docs/unified-backend-production-controlled-cutover-receipt-sample.json`。样板必须是普通 JSON，`mode` 固定为 `LOCAL_CONTROLLED_CUTOVER_RECEIPT_SHAPE_NOT_APPLIED`，`receiptApplied=false`，`productionTrafficAllowed=false`，`realValuesAllowedInRepository=false`。样板只允许记录审批引用、运行前置引用、灰度流量计划、切流执行引用、关键 smoke 引用、审计引用、观测引用、回滚窗口引用、`api-gateway` 流量引用、旧入口保护、脱敏规则、go/no-go 影响和验证命令，不得提交真实域名、真实 IP、真实 upstream、真实 token、真实 Cookie、真实内部签名、真实数据库连接串、真实 audit sink DSN、真实 dashboard 地址、真实日志索引、真实 topic、真实 bucket、真实部署命令、真实 SSH 命令或本地用户目录。

外部值接收演练正式样板文件为 `docs/unified-backend-production-external-value-intake-sample.json`。样板必须是普通 JSON，`mode` 固定为 `LOCAL_EXTERNAL_VALUE_INTAKE_REHEARSAL_NOT_APPLIED`，`intakeApplied=false`，`productionTrafficAllowed=false`，`realValuesAllowedInRepository=false`，`requiresExternalSecretStore=true`。样板只允许记录外部保管类型、值组、值 key、引用名、注入目标、验证引用、回滚引用、审批引用、脱敏规则和 go/no-go 阻塞项，不得提交真实 URL、真实 IP、真实域名、真实 token、真实 Cookie、真实内部签名、真实数据库连接串、真实 dashboard、真实部署地址、真实用户目录、真实终端命令或本地绝对路径。

## 候选 HTTP smoke

`POST /api/v1/unified-backend/admin/http-smoke/run`

成功响应 HTTP `200`。接口自身只在认证、权限、配置非法或内部状态不可用时返回错误。目标失败时返回统一成功响应，并在 `data.httpSmokeStatus` 中标记 `DEGRADED`。

smoke 目标至少包含 `UNIFIED_HEALTH`、`GATEWAY_HEALTH`、`BUSINESS_CORE_HEALTH`、`ADMISSION_CORE_HEALTH`、`ENGAGEMENT_CORE_HEALTH`、`OPS_CORE_HEALTH`、`PORTAL_CORE_HEALTH`、`AUTH_SESSION_VERIFY`、`PROFILE_MEMBERS`、`NOTIFICATION_UNREAD_COUNT`、`CONTENT_HOME`、`SERVER_STATUS_OVERVIEW`、`RESOURCE_LIST`、`ADMIN_OVERVIEW`、`ONBOARDING_PROGRESS`、`EXAM_SESSIONS`、`WHITELIST_CURRENT_APPLICATION`、`ATTENDANCE_LEADERBOARD`、`COMMUNITY_BOARDS`、`ACTIVITY_EVENTS`、`CALENDAR_UPCOMING`、`CHANGELOG_LATEST_VERSION`、`OPS_CONTROL_OVERVIEW`、`CLOUDREVE_SYNC_HEALTH`、`BACKUP_RECOVERY_HEALTH`、`ALERTING_HEALTH`、`PLUGIN_INTEGRATION_HEALTH`、`CROSS_PLATFORM_NOTIFICATION_HEALTH`、`OPS_IMAGE_MARKET_HEALTH`、`GUIDE_CATEGORIES`、`MATERIAL_FEATURED` 和 `ONLINE_MAP_HEALTH`。每个结果必须包含 `targetKey`、`serviceKey`、`method`、`path`、`mountDisposition`、`status`、`httpStatus`、`businessCode`、`durationMs`、`checkedAt` 和 `failureReason`。

响应不得返回完整请求头、Authorization、Cookie、token、异常栈、内部绝对路径、完整上游地址以外的本地环境路径或外部凭据。25 条业务路由必须通过本地控制器成功，不能调用 `GatewayHttpClient` 的 `AUTH`、`PROFILE`、`NOTIFICATION`、`CONTENT`、`SERVER_STATUS`、`RESOURCE`、`ADMIN`、`ONBOARDING`、`EXAM`、`WHITELIST`、`ATTENDANCE`、`COMMUNITY`、`ACTIVITY`、`CALENDAR`、`CHANGELOG`、`OPS_CONTROL`、`CLOUDREVE_SYNC`、`BACKUP_RECOVERY`、`ALERTING`、`PLUGIN_INTEGRATION`、`CROSS_PLATFORM_NOTIFICATION`、`OPS_IMAGE_MARKET`、`GUIDE`、`MATERIAL` 或 `ONLINE_MAP` 代理路径。

## 验收口径

`unified-backend` API 文档按 `docs/contracts-unified-backend.md` 独立存在，并由 `.local-docs/tests-unified-backend.md` 记录本地测试闭环。

完成时必须满足以下条件：`backend/unified-backend-service` 可独立运行测试；端口固定为 `8135`；五个自有 API 全覆盖认证、权限、成功路径、脱敏、挂载清单、readiness、sample 切流样板、本地切流演练执行证据、本地 file provider 演练 evidence、本地 audit sink adapter 演练 evidence、生产切换 runbook evidence、smoke 降级、真实 HTTP 演练、路由漂移扫描、后端单服务准备 evidence、最终单服务后端侧收束 evidence、单服务切换 evidence、入口切换适配 evidence、入口切换执行 evidence、生产入口切换 evidence、`api-gateway` 退役门禁 evidence、core 退役前置 evidence、生产化硬化前置 evidence、旧入口退役审批 evidence、集中配置治理 evidence、生产集中配置前置 evidence、持久化审计治理 evidence 和生产审计 sink 接入前置 evidence；25 条业务路由在候选入口内不经 `GatewayHttpClient` 代理即可成功响应；`/api/v1/gateway/**`、`/api/v1/business-core/**`、`/api/v1/admission-core/**`、`/api/v1/engagement-core/**`、`/api/v1/ops-core/**`、`/api/v1/portal-core/**`、`/api/v1/auth/**`、`/api/v1/profile/**`、`/api/v1/notifications/**`、`/api/v1/content/**`、`/api/v1/server-status/**`、`/api/v1/resources/**`、`/api/v1/admin/**`、`/api/v1/onboarding/**`、`/api/v1/exams/**`、`/api/v1/whitelist/**`、`/api/v1/attendance/**`、`/api/v1/community/**`、`/api/v1/activity/**`、`/api/v1/calendar/**`、`/api/v1/changelog/**`、`/api/v1/ops-control/**`、`/api/v1/cloudreve-sync/**`、`/api/v1/backup-recovery/**`、`/api/v1/alerting/**`、`/api/v1/plugin-integration/**`、`/api/v1/cross-platform-notification/**`、`/api/v1/ops-image-market/**`、`/api/v1/guides/**`、`/api/v1/materials/**` 和 `/api/v1/online-map/**` 不改路径、不改响应格式；当前 `api-gateway-service:8125`、`business-core-service:8130`、`admission-core-service:8131`、`engagement-core-service:8132`、`ops-core-service:8133` 和 `portal-core-service:8134` 测试继续通过；当前 6 个生产后端回滚入口和候选入口回归通过；外部节点执行器不进入候选入口源码扫描和 component scan；已退役旧服务目录、旧 Maven 入口、旧启动类和旧测试命令没有恢复；生产源码危险删除命令、真实节点执行、终端、RCON、Docker 执行和备份恢复写入扫描无命中。第三十五轮完成后只能说明 `unified-backend-service:8135` 已具备生产切换前的可执行 runbook evidence，候选入口覆盖、本地外部入口演练、本地配置 provider、本地 audit sink adapter、32 个 smoke 目标、7 个 Maven 回归入口、灰度计划、观测字段、回滚命令、退役顺序和删除禁令可自动验证；真实外部入口、真实集中配置 provider、真实 audit sink、生产 profile、敏感配置外置、生产流量切换、网关流量归零、回滚窗口和退役审批仍被阻塞。不能说明真实生产切流完成，不能说明前端或外部代理已经切换，不能说明真实集中配置已经接入，不能说明真实持久化审计已经接入，不能说明 `api-gateway` 或五个 core 可以批量删除，也不能说明外部节点执行器可以合并。
